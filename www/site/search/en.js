window.GOHUD_SEARCH=window.GOHUD_SEARCH||{};
window.GOHUD_SEARCH["en"]={"p":["index.html","install.html","ai.html","widgets.html","widgets-surfaces.html","widgets-messages.html","widgets-hud.html","widgets-feedback.html","widgets-forms.html","widgets-shapes.html","widgets-overlays.html","widgets-style.html","theming.html","theming-presets.html","theming-tokens.html","theming-opacity.html","theming-skins.html","theming-own.html","theming-contrast.html","icons.html"],"n":["Overview","Install","Go HUD Skill","Widgets","Widgets-Surfaces","Widgets-Messages","Widgets-Hud","Widgets-Feedback","Widgets-Forms","Widgets-Shapes","Widgets-Overlays","Widgets-Style","Theming","Theming-Presets","Theming-Tokens","Theming-Opacity","Theming-Skins","Theming-Own","Theming-Contrast","Icons"],"d":[[0,"showcase","What it looks like in a game",0,"The medieval and sci-fi presets — character sheets, inventories and quests; forms, grids and prompt cards; angular panels and touch controls. Switching presets is one line of code. Medieval — character, inventory and quests Medieval — forms, grids and prompt cards Sci-fi — angular panels and touch controls All six presets and how to switch them →","",-1],[0,"what","What it takes off your hands",0,"What costs the most time in game UI is not the shape of a button — it is everything that drifts apart between devices. gohud handles that drift in one place. Every measurement is a token Spacing, corners and text sizes are called by name, not written as numbers. Swap the theme and the whole screen moves with it. Visible size ≠ pressable size A close button looks 36dp but its touch target is 48dp. Where crowded slots overlap, the one whose centre is nearer takes the press. Notches and keyboards HUD pieces sit inside the safe area, and an input rises above the virtual keyboard when it appears. Back closes one thing Escape and Android Back close only the topmost surface. Stacked windows do not vanish together. Notices never steal input Press through a snackbar and the button underneath receives it. Focus is not taken either. Icons are asked for by name Widgets only know GoIconSet.CLOSE. Swap the set and every drawing changes without touching a line of code. Icons → Six looks, one line GoUi.use_preset() swaps theme, skin and icons together — rounded, chamfered sci-fi or forged medieval frames. Legible by measurement Every theme is checked against WCAG, including button states and translucent HUD panels over pure white and pure black. Everything works without enabling the plugin. Enabling it adds a GoRuntime autoload that tracks window size, dp scale and keyboard height, plus project settings for the config resource and the preset — conveniences, not requirements.","",-1],[0,"install","Install gohud",0,"Two ways in. Let your AI agent do it, or drop the folder into your project — there is nothing else to set up. Install","",-1],[0,"start","Quick start",0,"Five lines put a themed screen on the display. See the whole walk-through on the install page. Quick start","",-1],[0,"presets","Change the look — colours and shape",0,"A Theme can only restyle what the engine draws. Rounded corners are the only corners StyleBoxFlat has, and the joystick, quick slots and coach mark are drawn by code — so a theme alone can never change their shape. gohud therefore ships presets: a theme, a skin and an icon set picked as one unit. Preset Look default_dark The original gohud — rounded corners, soft blue accent default_light The same shapes on a light palette scifi_dark Chamfered corners, cyan neon edges and glow, hexagonal joystick, targeting brackets scifi_light The same angular shapes in a bright blueprint palette medieval_dark Dark iron and leather, antique-gold frames, rivets, engraved icons, Cinzel headings medieval_light Parchment, ink and bronze with the same forged frames default_dark — rounded corners, soft blue accent default_light — same shapes, light palette scifi_dark — chamfered corners, cyan neon scifi_light — same angular shapes, blueprint palette Those four are the same gallery screen. Not a line of code differs — only the preset name. Button corners, toggle shapes, list rows and input borders all move together.","GoUi.use_preset(GoThemePresets.MEDIEVAL_DARK) # theme, skin and icons together",-1],[0,"medieval-collection","Medieval collection",1,"medieval_dark pairs iron and leather with antique gold; medieval_light uses parchment and ink. Menu frames carry a rivet and a small corner ornament while the always-visible HUD stays quiet; 16 engraved icons redraw the item names, and only titles and subtitles use the bundled Cinzel font. medieval_dark — iron and leather medieval_light — parchment and ink Both shots are examples/medieval/medieval.tscn, built from the standard widgets. The medieval guide and how to make your own kingdom →","",4],[0,"the-same-screen-on-a-phone-and-on-a-desktop","The same screen on a phone and on a desktop",1,"The gallery shots are a phone in portrait. Below is the same code at 1280×800 — the content stops at a readable width (480dp) and centres, while the HUD stays in the corners. The form steps around floating HUD pieces, but does not move when it is already clear of them: pushing anyway would throw the content off-centre on a wide screen. default_dark · 1280×800 scifi_dark · 1280×800","",4],[0,"the-same-sheet-a-different-look","The same sheet, a different look",1,"Default — rounded card, sticky search row and footer Sci-fi — top accent edge, cut corners, glow Pick one from Project Settings → gohud → Theme → Preset, or set GoConfig.preset. Filling in theme, skin or icons explicitly wins over the preset, so you can take a preset and override just one of them. A new theme is one JSON file. new_theme.py kingdom --from medieval_dark writes a palette that spells out every inherited value; make_theme.py kingdom builds the theme, its control artwork and its skin dials, and the preset appears in the picker. Every setting and dial →","",4],[0,"skin","Skins — where a Theme cannot reach",0,"A skin owns the joystick, quick-slot faces, the coach-mark ring, chips, skeletons, alerts, dividers and section headings. Subclass it and override only what you want changed. Class Shapes it can make GoStyleBoxCut Diagonally cut corners (chamfer), one thickened accent edge, an outer glow GoStyleBoxBracket Corner marks only, without enclosing the content GoStyleBoxMedieval A forged frame with rivets, corner engraving, a bevel highlight and material grain All three serialise into a Theme resource — which is what lets a theme change shape, not just colour. GoSkinSciFi and GoSkinMedieval are the shipped subclasses. GoStyle.box() always returns a StyleBoxFlat, because calling code receives it and adjusts bg_color or corner_radius. Use GoStyle.surface() when a custom shape has to survive.","class_name MySkin extends GoSkin func slot_box(accent: Color, lit: bool) -> StyleBox: var box := GoStyleBoxCut.new() box.bg_color = accent box.cut = 6.0 box.edge_color = accent return box",-1],[0,"widgets","Widgets",0,"The pieces, in one table. Each one in detail → Class Base What it does GoSurface Control Floating card shell — centre, bottom or anchored placement, sticky header and footer, scrolling body, drag-to-resize GoSheet CanvasLayer Pages rising from the bottom, on their own layer so they sit above the HUD GoDialogs Node Confirmations and alerts you await; destructive for irreversible actions GoForm MarginContainer Width-capped forms that avoid the virtual keyboard and, with avoid_hud, the HUD GoScroll ScrollContainer Touch scrolling; the scrollbar tucks into the card padding GoNotice PanelContainer A snackbar that takes neither input nor focus GoPromptCard PanelContainer A question that does not block the game GoCoachMark Control A guided tour that points at real controls; pressing the target advances GoHudAnchor Control Pins HUD pieces to one of nine safe-area spots GoBar Control Health, mana and experience bars with eased changes GoSlot Button A quick slot — icon, quantity, cooldown and shortcut on one face GoJoystick Control Virtual stick in fixed, follow or relative mode GoIconButton Button Looks small, presses large GoStyle static Buttons, list rows, inputs, chips, tables, tabs and more, made one consistent way","",-1],[0,"waiting-telling-counting","Waiting, telling, counting",1,"Class Base What it does GoSnackbar Node the message that places itself GoSpinner Control a wait with no end in sight GoBadge PanelContainer the unread dot, the NEW tag, the 99+","",9],[0,"forms-and-lists","Forms and lists",1,"Class Base What it does GoField VBoxContainer the row that can be wrong GoInputGroup HBoxContainer an input welded to its button GoCombobox Button a picker that searches GoCodeInput VBoxContainer coupon and gift codes GoTable VBoxContainer sortable headers, selectable rows GoPagination HBoxContainer pages, or a More row","",9],[0,"shapes-games-actually-use","Shapes games actually use",1,"Class Base What it does GoRewardCalendar VBoxContainer daily attendance GoRadar · GoDonut Control stats at a glance GoCarousel VBoxContainer banners and character select GoKbd HBoxContainer key hints that do not lie","",9],[0,"over-the-screen","Over the screen",1,"Class Base What it does GoDrawer CanvasLayer the side panel GoPopover RefCounted the anchored card GoContextMenu RefCounted long-press and right-click GoConsole CanvasLayer the developer console","",9],[0,"ai","Go HUD Skill",0,"gohud ships an AI skill: the whole API, runnable templates and a preview launcher. Paste one block into your coding agent and it installs gohud and knows how to use it. Go HUD Skill","",-1],[0,"readable","Readability is measured, not eyeballed",0,"\"Pretty\" is taste; \"legible\" can be measured. Faint grey text looks fine on a good monitor and disappears on a phone in daylight — picking by eye guarantees it eventually. Every colour pair and every button state's label over the panel it sits on is measured against WCAG, in all six themes. Translucent colours are composited over their real backdrop first — measuring them directly reports a better ratio than the screen shows. Text colours are therefore derived, not chosen: the builder takes the palette entry as a starting point and pushes its lightness until it clears the threshold on every surface it can land on. Change the palette and the contrast follows. The rules and the traps behind them → Checks, releases and this site One entry point runs every check; each part sees something the others cannot. Check What it catches run_tests.sh at four screen sizes Widget behaviour and layout, RTL positions, keyboard focus, runtime skin contrast, every preset new_project_check.sh Hidden dependencies on a host project; with --zip the release as shipped, with --export a Web build check_contrast.py WCAG contrast of every theme file, button states and translucent panels included check_generated.py · check_scaffold.sh Generated themes match their palettes; a throwaway theme builds and passes contrast check_package.py The version taken from package.json, changelog moves and ZIP contents, in temporary copies check_site.py This site's links, anchors, page language, glossary and dial tables Latest recorded run — 2026-09-13, Godot 4.7.2, macOS, Compatibility renderer. 438 of 438 checks at 390×844, 844×390, 768×1024 and 1280×800 in an empty project; 438 of 438 again with the GoRuntime autoload; a Web export; zero contrast failures across the six themes. Godot 4.6 and physical Android or iOS devices were not part of this run.","python3 addons/gohud/tools/check_contrast.py bash addons/gohud/tools/check_all.sh",-1],[0,"release-versions","Release versions",1,"The version is the one written in package.json — packaging never raises it. To release a new version, change that number first. A successful run sets plugin.cfg and GoUi.VERSION to it and, the first time that version is packaged, moves the Unreleased notes of CHANGELOG.md into a dated entry. Packaging the same version again rebuilds its ZIP and replaces the old one. A failed run changes nothing. The ZIP leaves out this website, the tests, the tools and package.json.","cat addons/gohud/package.json # { \"version\": \"1.1.0\" } bash addons/gohud/tools/release.sh # every check → ZIP → that ZIP in an empty project bash addons/gohud/tools/package.sh # 1.1.0 → builds/1.1.0/gohud-1.1.0.zip",15],[0,"this-site","This site",1,"The pages are plain HTML in www/ — English at the root, Korean in ko/. A GitHub Actions workflow publishes that folder as the site root.","python3 addons/gohud/tools/make_site.py # glossary and skin-dial tables, generated from source python3 addons/gohud/tools/check_site.py # links, anchors, language, glossary, generated tables bash addons/gohud/tools/site_shots.sh /tmp/shots # desktop and phone screenshots of every page",15],[1,"ai-first","Fastest way — let your AI agent install it",0,"One block of text, pasted into Claude Code, Codex, Cursor or Gemini CLI, installs both the skill and the add-on and then explains gohud to you. Nothing below this card is needed if you take that road. Open the Go HUD Skill page →","",-1],[1,"install","Install",0,"Put the add-on folder into your project. There is nothing else to set up.","",-1],[1,"from-the-asset-store-or-a-release-zip","From the Asset Store or a release ZIP",1,"Extract into your project root so you get res://addons/gohud/. That is all. Optionally enable Project → Project Settings → Plugins → gohud.","",19],[1,"as-a-git-submodule","As a git submodule",1,"The repository root is the add-on folder, so it lands exactly where Godot expects it. Edit in place, commit and push inside addons/gohud, then commit the submodule pointer in your game. Godot 4.6 or newer is required. gohud uses APIs introduced in 4.5 (DPITexture, FoldableContainer, accessibility properties), so on anything older it fails while parsing. 4.6 is the supported floor, recorded in GoUi.MIN_ENGINE.","git submodule add https://github.com/thruthesky/gohud.git addons/gohud git submodule update --init git -C addons/gohud checkout v1.0.0 # optional: pin a tag or commit",19],[1,"start","Quick start",0,"Every widget is .new() and add_child() — no scene files, no autoload.","",-1],[1,"pick-a-look-before-building-ui-optional","Pick a look before building UI (optional)",1,"","GoUi.use_preset(GoThemePresets.SCIFI_DARK) # default_dark is used when you skip this",22],[1,"a-confirmation-you-await","A confirmation you await",1,"Pass destructive = true (the last argument) and the confirm button is drawn as a filled danger button.","var dialogs := GoDialogs.new() add_child(dialogs) if await dialogs.confirm(\"Delete save\", \"This cannot be undone.\"): delete_save()",22],[1,"a-bottom-sheet-with-a-sticky-search-field-and-fo","A bottom sheet with a sticky search field and footer",1,"Why sticky rows are separate from the scrolling body. Put confirm and cancel in body and a long list pushes them off screen. footer() is always visible.","var sheet := GoSheet.new() add_child(sheet) sheet.open(\"Inventory\") sheet.toolbar().add_child(GoStyle.line_edit(\"Search…\")) sheet.toolbar().visible = true for item in items: sheet.body.add_child(GoStyle.list_button(GoIconSet.BOX, item.name, _use.bind(item), Color.TRANSPARENT, item.description, false)) sheet.footer().add_child(GoStyle.button(\"Close\", sheet.close, GoStyle.Tone.PRIMARY)) sheet.footer().visible = true",22],[1,"a-hud-corner","A HUD corner",1,"","var corner := GoHudAnchor.new() corner.spot = GoHudAnchor.Spot.TOP_LEFT corner.landscape_spot = GoHudAnchor.Spot.TOP_RIGHT add_child(corner) var hp := GoBar.new() hp.label_text = \"HP\" hp.ink = GoUi.color(GoTheme.DANGER_FILL) corner.add_child(hp) hp.set_values(320, 500)",22],[1,"content-that-keeps-clear-of-the-hud","Content that keeps clear of the HUD",1,"Each rectangle is avoided in whichever direction costs the least area — downwards on a portrait phone, sideways in landscape. How the HUD avoids itself →","form.avoid_hud = true # keep clear of every visible GoHudAnchor joystick_anchor.reserve_space = false # …except ones that only appear under a finger notice_anchor.avoid_peers = true # a toast settles below the fixed HUD instead of covering it",22],[1,"examples","Examples you can run",0,"Three ways to see gohud before writing a line — none needs a server or an autoload. Example How to open What it shows Gallery examples/gallery/gallery.tscn · F6 Every widget, a picker for every installed preset, and the full icon set Medieval examples/medieval/medieval.tscn · F6 A character sheet, satchel and quest journal; buttons switch iron and parchment and open item details Demo app cd examples/demo && godot A home screen opening the gallery, the 19-chapter guided tour, the showcase screen and the medieval look, with live widgets and one card per class The demo app needs no setup: if the add-on link or the import cache is missing it says so, repairs the folder and reopens itself. Its home screen opens each example inside the same window — press 1–4 or click a row, and the bar across the top brings you back. In the tour, C toggles Cinema mode, F11 fullscreen, Space pauses (or plays the current widget while exploring), the arrows change chapters and Escape returns to Start. Using an AI coding agent? Open all three with one command →","cd examples/demo && godot # home screen; links and imports on first run bash examples/demo/run.sh # the same app, from the add-on root bash examples/demo/run.sh -- --open=gallery # skip the home screen bash examples/demo/run.sh -- --explore=surfaces # open straight onto one tour chapter bash examples/demo/run.sh --record /tmp/gohud-demo.avi # 1080p / 60 fps recording of the tour",-1],[2,"copy","Copy this into your coding agent",0,"Open your Godot project in the agent, paste the block, send it. It installs the skill and the add-on, then tells you what gohud can do. Open a terminal in your Godot project folder and start your coding agent there — for example claude. Press Copy on the block below, paste it into the agent and send it. When it is done, restart the agent if it asks you to, then ask it: What can gohud do? The block is written in English on purpose — that is what every agent reads most reliably. You can ask your own questions in any language; the skill answers in the language you write in.","Install gohud for this Godot 4.6+ project, then use it for all game UI (menus, HUD bars, quick slots, dialogs, bottom sheets, forms, themes). 1. Install the gohud AI skill. Claude Code - run these two commands in a shell: claude plugin marketplace add thruthesky/gohud claude plugin install gohud@gohud Any other agent (Codex, Cursor, Gemini CLI, ...): git clone --depth 1 https://github.com/thruthesky/gohud.git /tmp/gohud Copy /tmp/gohud/skills/gohud into whichever of these folders this agent reads: ~/.claude/skills/gohud, .claude/skills/gohud, .agents/skills/gohud. 2. Install the add-on itself, unless addons/gohud/plugin.cfg already exists here. In a git repository: git submodule add https://github.com/thruthesky/gohud.git addons/gohud Otherwise copy the clone to addons/gohud without its .git folder. Then run: godot --headless --path . --import (If godot is not on PATH, ask me where Godot 4.6+ is.) 3. Check the result: addons/gohud/plugin.cfg exists and the import printed no \"SCRIPT ERROR\" or \"Parse Error\" lines. 4. A new skill loads when the agent starts. Tell me if I have to restart you, then tell me what gohud can do and how to preview it. Docs: https://thruthesky.github.io/gohud/",-1],[2,"claude-code","Or type two commands — Claude Code",0,"This installs the skill as a Claude Code plugin. In a terminal: Already inside Claude Code? Type these instead: Then restart Claude Code and type /gohud:features — if it lists gohud's features, the skill is in. The add-on itself still goes into addons/gohud: Install →","claude plugin marketplace add thruthesky/gohud claude plugin install gohud@gohud /plugin marketplace add thruthesky/gohud /plugin install gohud@gohud",-1],[2,"agents","Where each agent keeps skills",0,"Installing by hand instead? Put the skills/gohud folder in the place your agent reads. The first one that exists wins. Agent Where the folder goes How commands look Claude Code — plugin claude plugin install gohud@gohud /gohud:preview · /gohud:features Claude Code — skill only ~/.claude/skills/gohud /gohud preview · /gohud features Claude Code — this project only .claude/skills/gohud /gohud preview Codex · Cursor · Gemini CLI — others .agents/skills/gohud —","",-1],[2,"commands","The commands you get",0,"","",-1],[2,"gohud-preview-the-widget-gallery","/gohud:preview — the widget gallery",1,"Opens the gallery: every widget, a picker for all six presets and the full icon set. Type it inside your Godot project and it uses your addons/gohud; without one it uses the copy bundled with the plugin, or a fresh clone. It runs in a throwaway sandbox project, so your autoloads and import cache are left alone, and the window stays open until you close it.","",32],[2,"gohud-preview-demo-the-19-chapter-guided-tour","/gohud:preview demo — the 19-chapter guided tour",1,"Opens the demo on its start screen. Start demo plays the tour with real input; Explore widgets lets you try one widget by hand. The keys listed under Examples work here too.","",32],[2,"many-other-commands","Many other commands",1,"Command What it does /gohud:preview medieval The character sheet, satchel and quest journal in the medieval presets /gohud:preview demo --explore hud The demo opened straight onto one chapter — hud buttons inputs selection lists data states surfaces prompt touch coach scrolling forms anchors theming /gohud:preview gallery --preset scifi_dark The gallery in one preset; add --phone for a 390×844 phone window or --size 1920x1080 for any size /gohud:preview res://ui/main_menu.tscn A scene of your own project, run inside that project /gohud:preview list Every target, preset id and demo chapter key /gohud:preview gallery --check A headless smoke test with no window — fails on script errors /gohud:features Every feature, grouped, one line of code each /gohud:features hud One area in depth — surfaces hud style theming icons i18n accessibility config You need Godot 4.6 or newer on your PATH for the preview commands.","",32],[2,"ask","Or just ask",0,"Or just ask. The skill teaches the agent gohud's whole API, so plain requests work too — \"Build a pause menu with gohud\". Installed as a plain skill in ~/.claude/skills/gohud, the commands take a space: /gohud preview demo, /gohud features. Previews need Godot 4.6 or newer on your PATH, or pass --godot /path/to/godot.","",-1],[4,"gosurface-the-shell","GoSurface — the shell",1,"Popups, sheets and dropdowns all use this one. It handles the safe area, the virtual keyboard, sticky header and footer, a scrolling body, which window is topmost, focus restoration, back-button ownership and drag-to-resize. Placement Looks like Used for CENTER A card in the middle Confirmations, settings BOTTOM A sheet rising from below Lists, management pages ANCHOR A card pinned beside a control Dropdowns, context menus It does not remove itself. Reacting to close_requested — hiding or freeing — is the owning screen's job, because the shell cannot know why it is closing (save and close, or discard).","",-1],[4,"gosheet-a-page-from-the-bottom","GoSheet — a page from the bottom",1,"It owns a CanvasLayer, so it reliably sits above the HUD. open() clears the body and turns off the back button and sticky rows — a dead button on a screen with nowhere to go back to reads as broken.","var sheet := GoSheet.new() add_child(sheet) sheet.open(\"Inventory\") sheet.toolbar().add_child(GoStyle.line_edit(\"Search…\")) sheet.toolbar().visible = true sheet.footer().add_child(GoStyle.button(\"Close\", sheet.close, GoStyle.Tone.PRIMARY)) sheet.footer().visible = true",-1],[4,"godialogs-confirmations-you-await","GoDialogs — confirmations you await",1,"confirm(title, body, ok_text, cancel_text, extra, args, destructive) — everything after the body is optional, and alert() works the same way with a single button. Set destructive for something that cannot be undone: the confirm button becomes the filled GoStyle.Tone.DANGER_SOLID button. On a light theme a tinted danger label has to darken until it reads as black text; the filled button keeps white text on red. The tone is re-applied on every call because one window is reused, and a second confirm() while one is open returns false at once. confirm_key() takes translation keys. Button layout. action_layout is VERTICAL by default (safe for long translations), HORIZONTAL for one row with half the width each, or AUTO, which uses one row only when both labels fit half the card on a single line and stacks them otherwise. action_gap is the space between the buttons and body_gap the space above them — make it wider than the button gap so the question and the choice read as two parts. For one dialog only, call set_next_action_layout() before opening it. The row is built when a dialog first opens, so an autoloaded GoDialogs adds nothing at startup.","if await dialogs.confirm(\"Delete save\", \"This cannot be undone.\"): delete_save()",-1],[4,"goform-a-width-capped-form","GoForm — a width-capped form",1,"It guarantees its descendant labels wrap. Without that, one long sentence stretches into a single line whose minimum width exceeds the screen, and the sides are clipped — you cannot even tell what screen you are on. Turning wrapping on by hand per scene is forgotten every time.","",-1],[4,"goscroll-touch-scrolling","GoScroll — touch scrolling",1,"Put one thing inside it. Headers and button rows belong outside — a long list must not push them off screen. use_panel_edge() moves the scrollbar into the card's existing padding while the content keeps its indent.","",-1],[5,"gonotice-snackbar","GoNotice — snackbar",1,"It never intercepts input. A notice is read, not pressed. It takes no focus, does not block game input, and the button beneath it still presses. When confirmation is needed use GoDialogs; when a choice is needed, GoPromptCard.","",-1],[5,"gopromptcard-a-question-that-does-not-block","GoPromptCard — a question that does not block",1,"Floats at one side with no scrim. The game keeps running.","",-1],[5,"gocoachmark-guided-tour","GoCoachMark — guided tour",1,"Points at real controls one at a time. Only the card takes input, so pressing the highlighted control itself advances the tour — \"press here\" becomes an action rather than a sentence. title and body may be translation keys or plain text. A step whose target has disappeared is skipped.","tour.start([ {\"target\": bag_button, \"title\": \"Bag\", \"body\": \"Everything you pick up lands here.\"}, {\"target\": map_button, \"title\": \"Map\", \"body\": \"Press to open the full map.\"}, ])",-1],[6,"the-small-things-decide-whether-it-looks-good","The small things decide whether it looks good",1,"Polish the big panels all you like — what catches the eye is always something small: a word split in two, a glow chopped off, a cramped slot. Four such things were pointed out on 2026-09-13 and fixed in the widgets and themes, not in the demo screen, so every screen gets the same result. What showed Cause Fix The coach mark's Done became Don/e A button with wrapping on subtracts its text width from its minimum — squeeze it and it breaks inside a word GoStyle.fit_words(): one word never wraps; several words wrap, but the longest word always fits — judged on the displayed text, not the translation key, and re-applied by the form when the language changes A full-width button's glow cut off vertically on the left A scroll container clips at its edge, no exceptions. Only the right survived, thanks to the rail gutter GoScroll borrows the parent's padding to push its edge 12dp out and gives it back inside — content stays put Time, icon and count stacked vertically in a slot Three rows inside 48dp Icon large in the centre, shortcut top-left, count as a bottom-right badge, remaining time as a badge over the icon — badge panels come from the skin's badge_box() A flat dropdown with no visible selection Item hover used the button panel; radio marks were the engine's faint default A dedicated hover panel (soft accent tint), the same radio artwork as checkboxes, item height sized for touch Dropdown — hover panel and radio mark Slots — corner badges and remaining time (sci-fi) Give icon-only buttons a description with GoStyle.icon_button(icon, action, -1, tooltip_key) — the tooltip for mouse users and the accessibility name for screen readers both come from that one value. gohud draws the tooltip itself: the engine's default splits the text one character per line when its width calculation goes wrong (measured at 1dp wide). The coach-mark card keeps off fixtures In landscape the card used to be sent to the very top or bottom edge — exactly where headers and HUD pieces live; in the demo it sat on the header's → ×. It now stays level with its target, steps around any GoHudAnchor that reserves space, and takes other things to avoid via keep_clear. It moves the shortest distance that clears the obstacle without covering the target, and stays put when the screen is too small to clear everything — overlapping beats vanishing. Its depth — so it reads as above the page — comes from skin dials (float_shadow_size, float_shadow_lift, float_shadow_alpha; chamfered skins use float_glow_size).","tour.keep_clear = [header_bar, toolbar] # fixtures that are not anchors",-1],[6,"screens-with-a-mouse-and-a-keyboard","Screens with a mouse and a keyboard",1,"A finger has no hover state. Build against mobile only and two desktop-only states go unchecked — and one of them was broken: gohud's focus ring was invisible on a primary button. The panel is the accent colour and so was the ring: 1.00:1, the same colour drawn over itself. Moving by keyboard, there was no way to tell where you were. Top to bottom: normal · hover · focus. Filled buttons get a ring that contrasts with their panel. Filled buttons (GoPrimaryButton, GoDangerSolidButton) use a ring that contrasts with their own panel — dark on a dark theme, white on a light one, a dark targeting bracket in sci-fi. The contrast checker measures the focus ring against the panel it sits on, so the rule survives a palette change (WCAG 2.4.11 asks for 3:1 on focus indicators).","",-1],[6,"hud-widgets","The HUD widgets",0,"","",-1],[6,"gohudanchor-nine-spots","GoHudAnchor — nine spots",1,"Respects the safe area and edge margins. Give it a landscape_spot and it moves in landscape — controls that sit bottom-centre in portrait can go bottom-right instead. Why it exists. Written by hand per HUD piece, anchors and offsets drift: a health bar ends up under the camera cutout on a notched device, and a control button lands on the gesture bar in landscape. That arithmetic lives in one place. Keeping content out from under the HUD When a scrolling screen shares the display with a HUD, the content slides underneath it and the two sets of text overlap until neither can be read. It happened twice in this add-on: an input's placeholder squeezed out between the quick slots, and a settings toggle's knob landed on the health bar. Each rectangle is avoided in whichever direction costs the least area. A bar in the top-right is stepped around downwards on a portrait phone (13% of the height) and sideways in landscape (21% of the width) — avoiding it vertically in landscape would cost 27% of the screen. Ground already given up is subtracted before the next rectangle is measured. HUD pieces collide with each other too The nine spots divide the screen; they do not guarantee the pieces miss each other. A wide snackbar at top-centre landed squarely on the health bar at top-right, hiding the value — you could not read how much health was left. It steps aside vertically only — moving sideways as well would make a centred toast appear in a different place each time it shows. And it only avoids pieces that are not stepping aside themselves: if both yield, the two swap places forever.","form.avoid_hud = true # keep clear of every visible GoHudAnchor joystick_anchor.reserve_space = false # …except ones that only appear under a finger notice_anchor.avoid_peers = true # settles below the fixed HUD notice_anchor.reserve_space = false # and does not push the page while it is up",47],[6,"gobar-health-mana-experience","GoBar — health, mana, experience",1,"Changes ease over 0.18s — a health bar that jumps does not tell you how hard you were hit. reduce_motion makes it instant. Large numbers abbreviate to 12.3k so the bar's width does not jitter as digits come and go.","var hp := GoBar.new() hp.label_text = \"HP\" hp.ink = GoUi.color(GoTheme.DANGER_FILL) # the fill-only token hp.set_values(320, 500) # \"320 / 500\"",47],[6,"goslot-one-quick-slot","GoSlot — one quick slot",1,"Icon, quantity, cooldown and shortcut on a single face. Hanging badges off a small circle makes each slot a different width and the row wobbles. Visible size and touch size differ. Packed slots have overlapping 48dp touch boxes; the overlap goes to whichever slot's centre is nearer — tell them about each other through touch_peers. keyboard_focus is off by default, so a row of eight slots does not sit between a keyboard user and every settings control in the Tab order. Turn it on when a keyboard or gamepad is the only input.","",47],[6,"gojoystick-virtual-stick","GoJoystick — virtual stick",1,"Mode Behaviour FIXED Always the same place — easy to find without looking FOLLOW Appears where the thumb lands RELATIVE Keeps following the thumb after appearing — comfortable for long movement The Vector2 from moved has length 0–1, so you can multiply it straight into a speed; it does not vary with screen size or DPI.","",47],[6,"goiconbutton-looks-small-presses-large","GoIconButton — looks small, presses large",1,"Only safe while its siblings ignore input. If the widened area covers the next button, that button stops responding. Side-by-side icon buttons need touch_peers.","",47],[7,"gosnackbar-the-message-that-places-itself","GoSnackbar — the message that places itself",1,"Sits at the bottom (or the top), above the safe area and the keyboard, and leaves on its own. Without it you write the layer, the placement, the timer and the teardown on every screen that has to say \"saved\". Messages queue instead of overwriting each other, and repeats of the same line are merged — a server failing four times a second no longer stacks four minutes of alerts. A snackbar with no button lets input through, so the game underneath keeps running. It is not a place to ask something. It goes away by itself, so the player may never see it. Irreversible confirmations belong in GoDialogs. Buttons here must be optional — Undo, Details, Retry. GoNotice is the other half of the pair: it never takes input or focus, and the screen that owns it decides where it goes. It cannot hold a button; this can.","snack.show_text(\"Saved\", GoTheme.SUCCESS) # A chance to undo — 0 is the first button, -1 means it timed out if await snack.post({\"text\": \"Item dropped\", \"actions\": [\"Undo\"]}) == 0: restore_item()",-1],[7,"gospinner-a-wait-with-no-end-in-sight","GoSpinner — a wait with no end in sight",1,"Games that talk to a server need this more than anything else on this page: a screen that sits still reads as a crash. One call turns the button you pressed into a spinner in place. The button keeps its size — the label goes transparent rather than away, so the row does not jump — and it is disabled while it spins. That second part is the point: a purchase must not fire twice while you await. With reduce_motion on it does not spin; three dots pulse instead. Progress you can measure is a GoBar. A spinner promises nothing about when it ends.","GoSpinner.busy(buy_button, true) var ok := await server.purchase(item) GoSpinner.busy(buy_button, false)",-1],[7,"gobadge-the-unread-dot-the-new-tag-the-99","GoBadge — the unread dot, the NEW tag, the 99+",1,"It hangs on the host's top-right corner with anchors, so it follows a host whose size is decided later, and attaching twice returns the same badge. Use a number when the count changes what the player does, a dot when only \"there is something\" matters. Its text colour is chosen for contrast against whatever colour the skin painted the badge — never assumed white — and it carries an accessible name, because a coloured dot says nothing to a screen reader.","GoBadge.attach(mail_button, unread_count) # hides itself at zero GoBadge.attach(shop_button, 0, \"NEW\") GoBadge.attach(friend_button, 3, \"\", true) # a dot — \"something is there\"",-1],[8,"gofield-the-row-that-can-be-wrong","GoField — the row that can be wrong",1,"One line at the top of a five-field form saying \"check your input\" does not say which field. That single thing is what makes people abandon a sign-up. The hint hides while an error shows, so the row does not grow by a line and push everything below it, and the error text is joined into the control's accessible name — a red border alone says nothing to someone who cannot see red. GoStyle.field() is the static version: label, control, hint, no error state. Use the factory when nothing can go wrong and the node for anything a server can reject.","var name_field := GoField.make(\"Character name\", GoStyle.line_edit(\"2-12\"), \"Cannot be changed later\") name_field.set_error(\"That name is taken\") # the server said no name_field.clear_error()",-1],[8,"goinputgroup-an-input-welded-to-its-button","GoInputGroup — an input welded to its button",1,"What it solves is corners. Two rounded shapes meeting in the middle look pinched, and the default gap makes them read as two objects. Outer corners stay round, touching ones go square — in every button state, or the group splits the moment it is pressed. Chamfered skins have no corner fields at all, so there it leaves them alone and the square faces meet cleanly.","GoInputGroup.make(GoStyle.line_edit(\"Message\"), {\"suffix\": GoStyle.icon_button(&\"send\", send)}) GoInputGroup.make(GoStyle.line_edit(\"Name\"), {\"prefix_icon\": &\"search\"})",-1],[8,"gocombobox-a-picker-that-searches","GoCombobox — a picker that searches",1,"Under ten entries GoStyle.select() is better: one less tap and the whole list is visible. Past thirty, scanning is work. The search matches inside names, not just their start. Prefix matching finds almost nothing in Korean or Japanese lists, where the word that tells two items apart is rarely the first one.","var picker := GoCombobox.make(server_names, 0, \"Choose a server\") picker.picked.connect(func(i: int) -> void: connect_to(servers[i]))",-1],[8,"gocodeinput-coupon-and-gift-codes","GoCodeInput — coupon and gift codes",1,"A code is pasted from a message far more often than it is typed. Twelve real fields would break pasting at the first cell and lose characters to an IME, so one hidden field takes the text and the cells are drawn. ABCD-EFGH-IJKL loses its dashes on the way in.","var coupon := GoCodeInput.make(12, 4) coupon.completed.connect(func(code: String) -> void: server.redeem(code)) coupon.set_error(\"Already used\")",-1],[8,"gotable-sortable-headers-selectable-rows","GoTable — sortable headers, selectable rows",1,"numeric: true, or the ranking inverts. Compared as text, \"9124\" beats \"91240\" — and scores, gold and damage all have mixed digit counts. row_selected gives the index in your original array, not the sorted position. The sort direction is shown with a glyph, not only a colour, and whole rows are pressable so nobody has to hit one cell. GoStyle.table() remains the read-only grid.","var board := GoTable.make( [{\"text\": \"Rank\", \"width\": 56}, {\"text\": \"Name\"}, {\"text\": \"Score\", \"numeric\": true}], rows) board.row_selected.connect(func(i: int) -> void: open_profile(rows[i]))",-1],[8,"gopagination-pages-or-a-more-row","GoPagination — pages, or a More row",1,"Numbers are a mouse UI; on a phone the More row reads better. The current page stays centred in the window, so pressing next does not reshuffle every number. A total of 0 means \"unknown\" and only the arrows are drawn — it does not invent a page count it was not given.","var pager := GoPagination.make(1, 12, func(page: int) -> void: load_mail(page)) var more := GoPagination.more(load_next) # the mobile-friendly variant",-1],[9,"gorewardcalendar-daily-attendance","GoRewardCalendar — daily attendance",1,"A date picker is almost never useful in a game. A calendar shape is — for attendance rewards, where what matters is which day you are on, not which date it is. Only today's cell can be pressed — a button that does nothing when pressed reads as broken. Claimed, today and future differ by three things at once: a tick glyph, an accent border and dimming.","var attendance := GoRewardCalendar.make(days, claimed_until) attendance.claimed.connect(func(day: int) -> void: server.claim_day(day))",-1],[9,"goradar-and-godonut-stats-at-a-glance","GoRadar and GoDonut — stats at a glance",1,"Radar values are 0–1 because what counts as full is a game decision — raw 120 STR beside 45 INT would draw a lie. Donut values are raw and it divides for you; past five slices it collapses the rest, since a sixth slice is a thread nobody can read. The comparison line is dashed, not just another colour, and the legend prints the percentage in words.","var stats := GoRadar.make({\"STR\": 0.85, \"AGI\": 0.5, \"INT\": 0.3, \"VIT\": 0.7, \"LUK\": 0.45}) stats.set_compare(with_new_sword) # a dashed overlay var share := GoDonut.make([{\"label\": \"Physical\", \"value\": 620}, {\"label\": \"Magic\", \"value\": 340}]) card.add_child(share.legend())",-1],[9,"gocarousel-banners-and-character-select","GoCarousel — banners and character select",1,"It does not advance on its own unless you ask, and five seconds is the floor: a banner that changes every three takes the text away before it is read. Touching it resets the timer, and with reduce_motion on it never advances by itself. The dots are pressable at the touch minimum even though the drawn dot is small.","banners.set_pages([promo, event, pack]) banners.autoplay_seconds = 5.0 # off by default",-1],[9,"gokbd-key-hints-that-do-not-lie","GoKbd — key hints that do not lie",1,"A hard-coded \"Press F\" keeps saying F after the player rebinds — the most common lie in a PC UI. for_action() reads InputMap; if the action is bound to a pad instead, it returns empty and the widget hides, because \"None\" would be its own lie. On Android and iOS it hides itself.","hint.add_child(GoKbd.for_action(&\"interact\")) # reads the real binding",-1],[10,"godrawer-the-side-panel","GoDrawer — the side panel",1,"GoSheet comes from the bottom, which is right for a phone held upright. On a tablet or a desktop the same sheet covers the game; a drawer comes from the side instead. The panel reaches the screen edge but the content stays inside the safe area, or a rounded corner clips the first row. LEFT means screen-left even in Arabic; turn on follow_text_direction when \"the start side\" is the point, and it flips in RTL.","bag.side = GoDrawer.Side.RIGHT bag.open(\"Bag\")",-1],[10,"gopopover-the-anchored-card","GoPopover — the anchored card",1,"This is GoSurface's anchored placement with the layer, the surface, the anchor and the teardown already wired — the ten lines every game writes over and over. One is open at a time, because stacked cards bury the screen and nobody can tell which card belongs to which slot. The scrim is transparent: a card you opened to compare gear must not darken the gear. It is not a hover tooltip. Touch has no hover, so anything only a mouse could reveal is invisible to half your players.","GoPopover.open(slot, item_card(item))",-1],[10,"gocontextmenu-long-press-and-right-click","GoContextMenu — long-press and right-click",1,"PopupMenu is already in the engine. What is not is the gesture: half a second to open, cancelled the moment the finger moves, so scrolling a list never pops a menu. Pass a Callable instead of an array when the entries depend on who you pressed. A long press needs a second way in. Nobody discovers a hidden gesture — pair it with a visible ⋯ button (open_at()), an icon, or a first-run coach mark.","GoContextMenu.attach(item_slot, [ {\"text\": \"Use\", \"action\": use}, {\"separator\": true}, {\"text\": \"Drop\", \"action\": drop, \"danger\": true}, ])",-1],[10,"goconsole-the-developer-console","GoConsole — the developer console",1,"Cheat and debug commands, with a palette that filters as you type so nobody has to memorise them, and a history on the arrow keys. It sits above everything, so you can debug over a dialog. It refuses to open in a release build. debug_only is on by default; cheat commands in a player's hands end a game's economy on the first day.","console.register(\"give\", \"Grant an item: give <id> <count>\", give) console.toggle()",-1],[11,"list-rows-with-a-description","List rows with a description",1,"A row with a second line gets equal top and bottom padding, roomier than a single-line row, and the title/description pair stays vertically centred beside the icon — also when a container stretches the row taller, and after the description wraps onto several lines. The whole row remains one tap target. Two-line rows, slots and bars in the medieval example — the same factory calls under medieval_light.","GoStyle.list_button(GoIconSet.SWORD, \"Oathkeeper\", _inspect, Color.TRANSPARENT, \"Longsword / Attack 42\", false)",-1],[11,"flowing-rows","Flowing rows",1,"Put controls into wrap_row at their natural width. With wrapping left on, a button's minimum width drops to almost nothing and the row sizes its cell by it — the label collapses to one character per line. The factory prevents this for you.","",-1],[11,"never-leave-an-empty-list-empty","Never leave an empty list empty",1,"People read a blank screen as broken.","page.add_child(GoStyle.empty_state(GoIconSet.BOX, \"Nothing here yet\"))",-1],[11,"hooks","Subclass hooks",0,"Every place a child widget is constructed goes through an overridable method, so a project that subclasses gohud types can get its own subclasses inside the widgets without copying code. Hook Widget Default _make_scroll() GoCoachMark, GoSurface GoScroll.new() _make_close_button() GoPromptCard, GoSurface GoIconButton.new() _make_surface() GoSheet, GoDialogs GoSurface.new() _should_pause() GoCoachMark Hides the card while a modal is open","",-1],[13,"pick","Picking one",0,"That one line swaps theme, skin and icons. From the editor, use Project Settings → Gohud → Theme → Preset; from a settings resource, GoConfig.preset.","GoUi.use_preset(GoThemePresets.SCIFI_DARK)",-1],[13,"keeping-one-field-your-own","Keeping one field your own",1,"Setting theme, skin or icons explicitly wins over the preset.","GoUi.use_preset(GoThemePresets.SCIFI_DARK) GoUi.config.icons = my_icons # your icons, the preset's everything else",74],[13,"the-six-built-in-presets","The six built-in presets",1,"Preset Panels Skin · icons default_dark · default_light Rounded StyleBoxFlat panels on a graded shadow scale; primary buttons lift and sink when pressed GoSkin · 84 default icons scifi_dark · scifi_light Chamfered GoStyleBoxCut panels with a neon edge and glow; GoStyleBoxBracket focus marks GoSkinSciFi — hexagonal joystick · default icons medieval_dark · medieval_light Forged GoStyleBoxMedieval frames — iron and leather, or parchment — with rivets and corner ornaments on menus GoSkinMedieval · 16 engraved icons over the defaults","",74],[13,"medieval","Medieval: iron, leather and parchment",0,"medieval_dark pairs dark iron and leather with antique gold. medieval_light uses parchment and ink. Both use GoSkinMedieval, the existing widgets, and 16 original icons with engraving details; navigation icons fall back to the original set. Choose the preset before creating your UI. Explicit GoConfig.theme, skin or icons overrides still take precedence. Open res://addons/gohud/examples/medieval/medieval.tscn and press F6 for a working character sheet, satchel and quest journal. Its buttons switch variants and open item details. Iron and leather Parchment and ink Menu frames have a rivet and small corner ornament; the always-visible HUD keeps a quiet edge. HP, mana and stamina use red, blue and olive fills. Cinzel is scoped to titles and subtitles; body text keeps the host/default font. The bundled font includes its license at assets/fonts/cinzel/OFL.txt. The engraved set redraws bag, book, box, coin, crown, flag, heart, key, map, potion, shield, star, sword and user, and adds scroll and seal. Every other name falls back to the default set, so close and settings keep their default drawings in every preset. Cinzel covers Latin script; give localized headings a font of their own through shape.fonts. What the suite checks for both variants. The medieval skin is selected; menu and HUD panels are forged frames, with the ornament on menus only; titles use Cinzel while body text keeps the inherited font; engraved icons import as DPITexture; a real quick slot uses the skin; changing a copied skin's dials reaches the slot without touching the preset; and switching back restores the default and sci-fi geometry.","GoUi.use_preset(GoThemePresets.MEDIEVAL_DARK) # Or: GoUi.use_preset(GoThemePresets.MEDIEVAL_LIGHT)",-1],[13,"make-a-kingdom-of-your-own","Make a kingdom of your own",1,"JSON themes can inherit other JSON themes. Omitted values keep their parent's settings; circular inheritance is rejected. The generated preset keeps its parent's icon set. Setting Effect shape.material 0: iron · 1: leather · 2: parchment. Controls the surface marks. shape.grain_alpha Texture opacity. Use 0 for a clean surface. shape.ornament_scale · shape.bevel_strength Menu corner ornament size and bevel highlight intensity. shape.fonts Map title, subtitle, caption, body and button roles to addon-local res:// font files. Omitted roles keep their inherited font. skin.dials Quick-slot radius, rivet visibility, grain and highlight. The full table below lists each dial.","python3 addons/gohud/tools/new_theme.py kingdom --from medieval_dark # Edit themes/palettes/kingdom.json, then: python3 addons/gohud/tools/make_theme.py kingdom godot --headless --path . --import # In GDScript, before creating the widgets: GoUi.use_preset(&\"kingdom\")",77],[14,"colours","Colours",1,"Name Used for background The screen floor surface · surface_soft · surface_high Three panel layers — panel, card, raised cell border Card and button borders text · secondary · muted Three text levels — body, supporting, dimmed accent · on_accent The accent and the text placed on it success · warning · danger · info Status text colours *_fill The same statuses as a fill — bars and other large areas scrim · shadow · track The veil behind popups, shadows, the bar's trough Why *_fill exists. The same meaning, opposite requirements. warning used as text must be dark enough to read on a light background; used as an experience bar's fill it must be bright enough to notice. One token cannot be both — the light theme's bar went brown. A theme without the _fill variant falls back to the base name automatically, so themes that predate these tokens keep working.","",-1],[14,"measurements-dp","Measurements (dp)",1,"touch button_height gap_tiny gap_small gap gap_large padding padding_compact compact_padding_x compact_padding_y radius_small radius radius_large screen_margin scroll_deadzone scroll_edge scrollbar_width list_glyph icon_size notice_duration_ms","",-1],[14,"panel-opacity","Panel opacity (%)",1,"panel_alpha card_alpha hud_alpha notice_alpha popup_alpha — how solid a container's face is. 80 by default, so the game stays visible behind a dialog; popup_alpha is 100. A theme without these tokens falls back to 100 and draws exactly as it did before. Read the resolved value as a ratio with GoUi.surface_alpha(variant); the section Opacity — the game behind the panel has the rules.","",-1],[14,"text-roles","Text roles",1,"micro · compact · caption · body · button · subtitle · title — a role names a size, not a purpose. \"The largest text\", not \"the heading\". That way the scale holds even when a screen uses it for something else.","",-1],[14,"type-variations","Type variations",1,"GoPanel GoCard GoButton GoPrimaryButton GoDangerButton GoDangerSolidButton GoBareButton GoCompactButton GoIconButton GoListButton GoTitleLabel GoSubtitleLabel GoCaptionLabel GoCompactLabel GoMicroLabel","",-1],[15,"five-layers-most-specific-first","Five layers, most specific first",1,"Order Where Unit Reach ① the argument at that call — surface.alpha, GoStyle.card(…, alpha) ratio 0.0–1.0 one window ② GoConfig.container_alpha_overrides[kind] ratio 0.0–1.0 one kind of panel, project-wide ③ GoConfig.metric_overrides[<kind>_alpha] percent projects that keep every measurement in one place ④ GoConfig.container_alpha ratio 0.0–1.0 every panel at once ⑤ the theme's GoHud/constants/<kind>_alpha percent what the look decides — the source of truth With none of them set the value is 100, so nothing about an older theme changes. Opacity is a ratio everywhere — except in the theme. Every alpha field and argument takes 0.0–1.0, the same units as Color.a and modulate.a, and a negative value means \"not set\". The one exception is the theme's constants, because a Theme constant cannot hold a float: those and the channel that overrides them (metric_overrides, which shares the theme's names and integer type) are written as 80. Nothing else is.","# One window only — a confirm dialog that must not hide the fight behind it surface.alpha = 0.6 sheet.alpha = 0.7 dialogs.alpha = 0.9 # @export fields are ratios too drawer.alpha = 0.7 GoPopover.open(slot, body, {\"alpha\": 0.9}) var glass := GoStyle.card(Color.TRANSPARENT, -1.0, -1.0, -1.0, 0.5) # Per kind — keep the HUD nearly solid, because its text sits straight over the world GoUi.config.container_alpha_overrides = { GoTheme.BOX_PANEL: 0.7, # dialogs and sheets can breathe GoTheme.BOX_HUD: 0.95, } GoUi.refresh() # redraws widgets that are already open # The whole project — a busy world wants its panels back to solid GoUi.config.container_alpha = 1.0 GoUi.refresh() GoUi.surface_alpha(GoTheme.BOX_PANEL) # the resolved ratio, e.g. 0.8",-1],[15,"what-follows-the-value-and-what-does-not","What follows the value, and what does not",1,"Follows it — containers Ignores it — things you press, and markers GoSurface (the one shell behind dialogs, sheets and dropdowns) · GoSheet · GoDialogs · GoDrawer · GoPopover · GoNotice · GoSnackbar · GoPromptCard · GoCoachMark · GoConsole every button · GoSlot (quick slots) · GoBadge · segmented controls · choice cells · chips · discs and avatars GoStyle.card() · hud_panel() · overlay_panel() · alert() · plate() · edge_card_panel() · style_notice_panel() · floating() · box() · surface() all text and icons A button whose state is washed out no longer says what it is, and a badge exists to make two characters legible — neither is a container, so neither follows the value. Popup menus stay solid (popup_alpha 100). The engine may host a PopupMenu in its own Window, and there the OS does not composite it with the game: translucency comes out black rather than see-through. Lower it only in projects that embed their subwindows (gui_embed_subwindows).","",-1],[15,"three-details-that-come-from-real-screens","Three details that come from real screens",1,"The value multiplies, it does not replace. The default HUD face is already 0.92 alpha, so hud_alpha 80 lands at 0.736 — a theme that deliberately made a face translucent keeps that decision. The flip side: apply it twice and the panel fades twice, which is why the alpha is multiplied in exactly one place (GoSkin.surface_box()) and every other face function forwards the argument instead of fading its own result. A colour you passed is a colour you get. GoStyle.plate(…, fill) keeps an explicit fill exactly as given: Color(ink, 0.14) already states its alpha, and multiplying panel opacity into it would cut the caller's intent twice. GoStyle.floating(…, opaque = true) ignores the value for the same kind of reason — filling the face solid is that flag's whole purpose. Panels gohud did not build get the same rule. GoStyle.fade_panel(node) — after the node is in the tree, so it reads the theme it inherits. It is idempotent: the original face is recorded and every call recomputes from that, so repeated redraws never stack. After swapping themes, GoStyle.forget_face(node) drops that memory so the next call picks up the new face.","",-1],[15,"custom-faces-fade-too-and-that-is-the-part-that","Custom faces fade too — and that is the part that silently breaks",1,"The cut-corner panel (GoStyleBoxCut) and the forged frame (GoStyleBoxMedieval) are not StyleBoxFlat; they draw themselves. Put the opacity only in the flat branch and these themes quietly ignore it while every value-based test still passes. Both fade their background and keep glow, rivets, corner engraving and bevels at full strength — and the medieval face's grain and bevel were always proportional to its background alpha, so they thin out along with the panel. Which is the wider point: bg_color.a == 0.8 is a number. The world shows through and the text is still legible is the requirement. gohud checks the first in its headless suite and the second by drawing panels over diagonal stripes on a virtual monitor (tests/gohud_alpha_shot.gd) — on the flat, the cut and the forged face, in both the dark and the light theme.","",-1],[16,"shapes-styleboxflat-cannot-make","Shapes StyleBoxFlat cannot make",1,"Class What it draws GoStyleBoxCut Diagonally cut corners, one thickened accent edge, an outer glow GoStyleBoxBracket Corner marks only, leaving the content unenclosed GoStyleBoxMedieval A forged frame — rivets, corner engraving, a bevel highlight and iron, leather or parchment grain All three serialise into a Theme resource like any other StyleBox — which is what lets a theme change shape. Give padding through content_margin_*. Subclassing StyleBox in GDScript means _get_style_margin() is never called (measured on Godot 4.7). Fill the built-in fields or content will sit against the border.","",-1],[17,"what-you-can-change-on-one-page","What you can change — on one page",1,"Where What How from the parent theme dark · light · scifi_dark · medieval_dark · … or another JSON theme. Omitted keys inherit its values; an inheritance cycle fails palette background · three surfaces · border · three text tones · accent · four status colours · scrim · shadow · bar track \"#RRGGBB\", alpha as \"#RRGGBB@0.35\" fill-only vivid colours *_vivid keep bars saturated even when the text colour has to darken — leave one out and the builder says so readability not tuned by hand. Text, borders, the accent and the label on it are pushed by the builder until they clear the thresholds — change only the accent and the checks still pass shape kind flat (rounded) · cut (chamfered corners) · medieval (forged frames) sizes radius · radius_small · radius_large · gap · gap_small · gap_large · padding · button_height · button_padding[4] — the tokens follow, so panels and widget maths never disagree cut only cut_ratio · cut_max · corners (diagonal|all) · glow · edge medieval only material (0 iron · 1 leather · 2 parchment) · grain_alpha · ornament_scale · bevel_strength · fonts — title, subtitle, caption, body, button mapped to addon-local res:// font files skin base — the family for the places code draws itself (joystick, slots, badges, coach-mark ring, chips, dividers) default · scifi · medieval dials — the numbers of those places: slot border width and tint, badge padding, chip and alert opacity, joystick ring width and opacity, sci-fi chamfer sizes, glow and bracket Every parent value is spelled out in the JSON. The builder writes them into themes/skins/gohud_skin_<id>.tres — no skin code is touched. Only to change the drawings themselves do you run --new-skin, which scaffolds a GoSkin subclass icons the icon set a res:// path to a GoIconSet resource — kept from a JSON parent when omitted dark · title how the picker shows it boolean · string","",-1],[17,"every-dial","Every dial",1,"The table below is filled by tools/make_site.py straight from the skin scripts' @export lines — add a dial or change a default and the table follows. Use these names under skin.dials in the JSON. A dial whose default reads 14 is an integer, one that reads 10.0 a float — write either form in the JSON and the builder casts to the script's type. Default skin GoSkin — 17 Dial Default What it sets chip_fill_alpha 0.16 Fill opacity of a chip's panel. chip_edge_alpha 0.45 Edge opacity of a chip's panel. alert_tint 0.1 How far an alert box's panel is tinted toward its status colour. slot_tint_lit 0.24 Accent tint of a quick-slot panel while its cooldown runs. slot_tint_idle 0.08 Accent tint of a quick-slot panel at rest. slot_border_lit 2 Quick-slot border width (dp) while its cooldown runs. slot_border_idle 1 Quick-slot border width (dp) at rest. badge_pad_x 5 Horizontal inner padding (dp) of a badge (quantity, time left). badge_pad_y 1 Vertical inner padding (dp) of a badge. badge_edge_alpha 0.6 Edge opacity of a badge's panel. float_shadow_alpha 0.45 Shadow opacity of floating cards (coach mark, prompt card). float_shadow_size 14 Shadow blur (dp) of floating cards. float_shadow_lift 4 Downward offset (dp) of a floating card's shadow. float_glow_size 10.0 Glow distance (dp) of a floating panel that glows instead of casting a shadow, like the sci-fi chamfered panel. joystick_base_alpha 0.42 Opacity of the joystick's base disc. joystick_ring_alpha 0.45 Opacity of the joystick's ring. joystick_ring_width 2.0 Width (dp) of the joystick's ring. Sci-fi skin GoSkinSciFi — 10 Dial Default What it sets cut_chip 7.0 Chamfer size (dp) of a chip's panel. cut_skeleton 5.0 Chamfer size (dp) of a skeleton panel. cut_alert 8.0 Chamfer size (dp) of an alert box. cut_segment 8.0 Chamfer size (dp) of a segmented control. cut_slot 6.0 Chamfer size (dp) of a quick-slot panel. cut_disc_ratio 0.24 Chamfer of avatars and discs = diameter × this ratio. slot_glow_alpha 0.45 Glow opacity of a slot whose cooldown is running. slot_glow_size 6.0 Glow distance (dp) of a slot whose cooldown is running. bracket_arm 12.0 Arm length (dp) of the coach mark's targeting bracket. bracket_thickness 2.0 Line thickness (dp) of the coach mark's targeting bracket. Medieval skin GoSkinMedieval — 5 Dial Default What it sets slot_radius 4.0 Corner radius (dp) of the medieval quick-slot frame. leather_grain_alpha 0.035 Opacity of the subtle leather marks inside quick slots. ornament_scale 1.0 Scale of the metal decorations at quick-slot corners. Menu frames use shape.ornament_scale. bevel_strength 0.18 Strength of the metal highlight along quick-slot edges. slot_rivets 1 Show small metal rivets on quick slots: 0 hides them, 1 shows them.","",-1],[17,"subclass-the-skin-optional","Subclass the skin (optional)",1,"Only what theme and shape cannot reach — the joystick's hexagon, slot badges, the coach mark's targeting bracket — lives in a skin. Subclass GoSkin, override just those methods, and point the JSON's skin at that resource. A colour-only theme skips this step. A check keeps the promise. tools/check_scaffold.sh creates a throwaway theme every run — changes the accent, sets the radius to 4 — and verifies generation, token propagation and a clean contrast pass before removing it. The first run found a theme with only its accent changed failing 8 pairs: the builder pushed text colours but used the accent as written. It now pushes the accent and the label on it too.","",-1],[18,"same-hue-text-on-a-same-hue-tint","Same-hue text on a same-hue tint",1,"This trap appeared three times in this codebase — pressed buttons (accent panel, accent label), danger buttons, and chips. It looks good in a swatch and collapses the moment it is measured, because the two share a hue and so have no difference in lightness. Label colours are therefore not chosen by hand. The theme builder and GoSkin each push lightness — and only lightness — until the threshold is cleared on the panel the text actually sits on. Hue and saturation survive, so the palette keeps its character and changing it keeps its contrast.","",-1],[18,"colour-read-as-text-and-colour-painted-as-area-w","Colour read as text and colour painted as area want opposite things",1,"On a light theme a warning colour has to darken to stay readable as text (#96500A). When the bar fill inherits that, the experience bar turns brown. Colour alone cannot fix it: yellow is intrinsically bright, so it clears 3:1 over no grey track at all, and darkening the track makes it worse (1.52 → 1.06). So the two are separated. Colour stays vivid — the palette carries fill-only colours such as warning_vivid. The edge carries the contrast — GoSkin draws a 1dp outline on any fill that cannot reach 3:1 against its track, so \"how full is it\" stays legible. Leaving it out is reported. Without a *_vivid, a fill that comes out dark makes the builder print the bar looks drab — add warning_vivid to the palette. A place that used to degrade silently now says so.","MY_LIGHT = dict( warning=hexc(\"9A5407\"), # read as text warning_vivid=hexc(\"F2A007\"), # painted on the bar )",-1],[18,"a-floating-panel-cannot-know-what-is-behind-it","A floating panel cannot know what is behind it",1,"A HUD is drawn over the game — a snowfield, or a cave. If its panel is translucent, whatever is behind it bleeds through and changes the panel's colour, taking the text contrast down with it. At 82% opacity the dark themes' muted text fell to 3.74:1 over a bright backdrop — below the minimum. So translucent panels are measured with pure white and pure black behind them; those are the worst a game scene can offer. The bundled themes' HUD panel is opaque enough to pass (92%) while keeping the glassy look. For the same reason icon colours are not fixed either. The quick slot's glyph was hard-coded white and measured 1.11:1 on a light theme — it now follows the panel it sits on, exactly as the labels do. Two checkers, two reaches. check_contrast.py reads the theme files; colours a skin mixes while running (chips, slots) are measured by the suite's skin contrast section inside Godot.","",-1],[19,"names","Call an icon by its name",0,"Widgets never hold a picture. They ask for a name, and the icon set in use decides what is drawn — swap the set and every icon changes without touching a line of code. GoUi.icons() is the set in use. A name it cannot draw becomes an empty box that keeps its space, with a warning in debug builds, so a missing icon never shifts a row.","GoStyle.icon_button(GoIconSet.SETTINGS, open_settings) # a button slot.icon_name = GoGameIcons.BACKPACK # a quick slot var mark := GoUi.icons().node(&\"cloud_rain\", 24) # a bare Control, any size",-1],[19,"catalog","Find an icon",0,"Every drawing gohud ships. Search by name or by what it shows, narrow it to a set or a group, and pick an icon to copy its name. Search icons GroupAll groups All 1,287 Default 84 Game 187 Library 1,000 Medieval 16 Show more Names and search words stay English in every language — they are what you type in code.","",-1],[19,"sets","Four sets, one lookup",0,"Every set answers the same call. The big ones are added on top of the set in use. Set Names Constants Turn it on Default 84 GoIconSet Always on Medieval 16 engraved — GoUi.use_preset(GoThemePresets.MEDIEVAL_DARK) Game 187 GoGameIcons GoUi.add_icons(GoGameIcons.icon_set()) Library 1,000 GoIconLibrary GoUi.add_icons(GoIconLibrary.icon_set()) The library falls back to the game set and the game set to the default set, so one line makes 1,271 names drawable. A set lists its drawings as paths and reads a file only the first time its name is drawn: adding the library reads a table of paths (about 30 ms on a desktop), not 1,000 textures. Paths are strings, not dependencies. An export that picks resources must include addons/gohud/icons/; the default “export all resources” needs nothing.","GoUi.add_icons(GoIconLibrary.icon_set()) # the library — and the game set under it GoUi.add_icons(GoGameIcons.icon_set()) # or the game set alone",-1],[19,"resolution","How a name is found",0,"One order for every set, so you can tell which drawing wins. Every set's own drawings first — the set in use, then each extra set, then their fallbacks, level by level. A set's own drawing is a texture, a path or an icon-font glyph. If nothing drew it, an alias is tried: gear leads to settings, x to close. One hop only. Still nothing: an empty box that keeps its space. That order is why the medieval preset's engraved sword stays on top when you add the library — its own drawing is found before anyone's fallback.","GoUi.use_preset(GoThemePresets.MEDIEVAL_DARK) GoUi.add_icons(GoIconLibrary.icon_set()) GoUi.icons().texture(&\"sword\") # the medieval engraving — its own drawing comes first GoUi.icons().texture(&\"rocket\") # from the game set GoUi.icons().canonical(&\"gear\") # &\"settings\" — an alias",-1],[19,"swap","Presets, your set and extra sets",0,"Three places decide what is drawn. Call What it does GoUi.use_preset(…) Theme, skin and icon set in one go. Clears config.icons, keeps config.extra_icons. GoUi.config.icons = my_set Replaces the preset's icon set with yours. GoUi.add_icons(set) Adds a set that is looked in after the one in use. It survives preset changes.","",-1],[19,"custom","Your own drawings",0,"Override a few names, add a whole folder, or plug in an icon font — the calling code stays the same. Draw on a 24×24 grid with a white stroke. Color comes from modulate, which can only darken — a black drawing stays black on every theme. Import SVGs as DPITexture so they stay sharp in a 64 dp inventory slot. Icons never mirror in right-to-left languages. Only back and forward change direction, and you choose those by name. An icon font works too: fill font and codepoints. Nothing else changes.","# Override a few names — everything else still comes from the set in use var mine := GoIconSet.new() mine.fallback = GoUi.icons() mine.textures = {GoIconSet.CLOSE: preload(\"res://art/close.svg\")} GoUi.config.icons = mine # A folder of drawings — file name = icon name, each read when first drawn GoUi.config.icons = GoIconSet.from_folder(\"res://art/icons\", GoUi.icons())",-1],[19,"naming","Names, groups and search",0,"The rules the bundled sets follow, and the calls that find a name. lower_snake_case, the thing drawn first and its state last: volume_off, battery_charging, arrow_bar_left. No group prefixes. Groups are data, so a name stays the same wherever it is filed. Library names are Tabler's names with underscores: cloud-rain on tabler.io is cloud_rain here. Tabler's names for drawings gohud already had lead there as aliases (shopping_cart → cart). A synonym is an alias, never a second drawing.","",-1],[19,"search","search()",1,"","var icons := GoUi.icons() icons.search(\"arrow left\", 5) # best matches first: back, arrow_bar_left … icons.search(\"rain\") # cloud_rain, then drawings tagged \"rain\"",101],[19,"group-names-names-in-group","group_names() · names_in_group()",1,"","icons.group_names() # every group key, in lookup order icons.names_in_group(&\"weather\") # one group, in display order — every set's share of it icons.group_title(&\"weather\") # \"Weather & sky\"",101],[19,"canonical-has-icon","canonical() · has_icon()",1,"","icons.canonical(&\"gear\") # &\"settings\" icons.canonical(&\"treasure_chest\") # &\"chest\" — Tabler's name for a game drawing icons.has_icon(&\"cloud_rain\") # true once the library is added — nothing is loaded to answer",101],[19,"license","Where the drawings come from",0,"Everything is MIT. The notices travel in the ZIP as THIRD_PARTY_NOTICES.md. Set Source License Default Drawn for gohud MIT Medieval Drawn for gohud MIT Game 171 from Tabler Icons 3.46.0, 16 drawn for gohud MIT Library Tabler Icons 3.46.0 MIT If you plug in someone else's icons, write their source in GoIconSet.attribution — and keep commercial icon fonts inside your game, not in a fork you publish.","",-1]]};
