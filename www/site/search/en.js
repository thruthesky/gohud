window.GOHUD_SEARCH=window.GOHUD_SEARCH||{};
window.GOHUD_SEARCH["en"]={"p":["index.html","theming.html","widgets.html"],"n":["Overview","Theming","Widgets"],"d":[[0,"showcase","What it looks like in a game",0,"The medieval and sci-fi presets — character sheets, inventories and quests; forms, grids and prompt cards; angular panels and touch controls. Switching presets is one line of code. Medieval — character, inventory and quests Medieval — forms, grids and prompt cards Sci-fi — angular panels and touch controls All six presets and how to switch them →","",-1],[0,"what","What it takes off your hands",0,"What costs the most time in game UI is not the shape of a button — it is everything that drifts apart between devices. gohud handles that drift in one place. Every measurement is a token Spacing, corners and text sizes are called by name, not written as numbers. Swap the theme and the whole screen moves with it. Visible size ≠ pressable size A close button looks 36dp but its touch target is 48dp. Where crowded slots overlap, the one whose centre is nearer takes the press. Notches and keyboards HUD pieces sit inside the safe area, and an input rises above the virtual keyboard when it appears. Back closes one thing Escape and Android Back close only the topmost surface. Stacked windows do not vanish together. Notices never steal input Press through a snackbar and the button underneath receives it. Focus is not taken either. Icons are asked for by name Widgets only know GoIconSet.CLOSE. Swap the set and every drawing changes without touching a line of code. Six looks, one line GoUi.use_preset() swaps theme, skin and icons together — rounded, chamfered sci-fi or forged medieval frames. Legible by measurement Every theme is checked against WCAG, including button states and translucent HUD panels over pure white and pure black. Everything works without enabling the plugin. Enabling it adds a GoRuntime autoload that tracks window size, dp scale and keyboard height, plus project settings for the config resource and the preset — conveniences, not requirements.","",-1],[0,"install","Install",0,"Put the add-on folder into your project. There is nothing else to set up.","",-1],[0,"from-the-asset-store-or-a-release-zip","From the Asset Store or a release ZIP",1,"Extract into your project root so you get res://addons/gohud/. That is all. Optionally enable Project → Project Settings → Plugins → gohud.","",2],[0,"as-a-git-submodule","As a git submodule",1,"The repository root is the add-on folder, so it lands exactly where Godot expects it. Edit in place, commit and push inside addons/gohud, then commit the submodule pointer in your game. Godot 4.6 or newer is required. gohud uses APIs introduced in 4.5 (DPITexture, FoldableContainer, accessibility properties), so on anything older it fails while parsing. 4.6 is the supported floor, recorded in GoUi.MIN_ENGINE.","git submodule add https://github.com/thruthesky/gohud.git addons/gohud git submodule update --init git -C addons/gohud checkout v1.0.0 # optional: pin a tag or commit",2],[0,"start","Quick start",0,"Every widget is .new() and add_child() — no scene files, no autoload.","",-1],[0,"pick-a-look-before-building-ui-optional","Pick a look before building UI (optional)",1,"","GoUi.use_preset(GoThemePresets.SCIFI_DARK) # default_dark is used when you skip this",5],[0,"a-confirmation-you-await","A confirmation you await",1,"Pass destructive = true (the last argument) and the confirm button is drawn as a filled danger button.","var dialogs := GoDialogs.new() add_child(dialogs) if await dialogs.confirm(\"Delete save\", \"This cannot be undone.\"): delete_save()",5],[0,"a-bottom-sheet-with-a-sticky-search-field-and-fo","A bottom sheet with a sticky search field and footer",1,"Why sticky rows are separate from the scrolling body. Put confirm and cancel in body and a long list pushes them off screen. footer() is always visible.","var sheet := GoSheet.new() add_child(sheet) sheet.open(\"Inventory\") sheet.toolbar().add_child(GoStyle.line_edit(\"Search…\")) sheet.toolbar().visible = true for item in items: sheet.body.add_child(GoStyle.list_button(GoIconSet.BOX, item.name, _use.bind(item), Color.TRANSPARENT, item.description, false)) sheet.footer().add_child(GoStyle.button(\"Close\", sheet.close, GoStyle.Tone.PRIMARY)) sheet.footer().visible = true",5],[0,"a-hud-corner","A HUD corner",1,"","var corner := GoHudAnchor.new() corner.spot = GoHudAnchor.Spot.TOP_LEFT corner.landscape_spot = GoHudAnchor.Spot.TOP_RIGHT add_child(corner) var hp := GoBar.new() hp.label_text = \"HP\" hp.ink = GoUi.color(GoTheme.DANGER_FILL) corner.add_child(hp) hp.set_values(320, 500)",5],[0,"content-that-keeps-clear-of-the-hud","Content that keeps clear of the HUD",1,"Each rectangle is avoided in whichever direction costs the least area — downwards on a portrait phone, sideways in landscape. How the HUD avoids itself →","form.avoid_hud = true # keep clear of every visible GoHudAnchor joystick_anchor.reserve_space = false # …except ones that only appear under a finger notice_anchor.avoid_peers = true # a toast settles below the fixed HUD instead of covering it",5],[0,"presets","Change the look — colours and shape",0,"A Theme can only restyle what the engine draws. Rounded corners are the only corners StyleBoxFlat has, and the joystick, quick slots and coach mark are drawn by code — so a theme alone can never change their shape. gohud therefore ships presets: a theme, a skin and an icon set picked as one unit. Preset Look default_dark The original gohud — rounded corners, soft blue accent default_light The same shapes on a light palette scifi_dark Chamfered corners, cyan neon edges and glow, hexagonal joystick, targeting brackets scifi_light The same angular shapes in a bright blueprint palette medieval_dark Dark iron and leather, antique-gold frames, rivets, engraved icons, Cinzel headings medieval_light Parchment, ink and bronze with the same forged frames default_dark — rounded corners, soft blue accent default_light — same shapes, light palette scifi_dark — chamfered corners, cyan neon scifi_light — same angular shapes, blueprint palette Those four are the same gallery screen. Not a line of code differs — only the preset name. Button corners, toggle shapes, list rows and input borders all move together.","GoUi.use_preset(GoThemePresets.MEDIEVAL_DARK) # theme, skin and icons together",-1],[0,"medieval-collection","Medieval collection",1,"medieval_dark pairs iron and leather with antique gold; medieval_light uses parchment and ink. Menu frames carry a rivet and a small corner ornament while the always-visible HUD stays quiet; 16 engraved icons redraw the item names, and only titles and subtitles use the bundled Cinzel font. medieval_dark — iron and leather medieval_light — parchment and ink Both shots are examples/medieval/medieval.tscn, built from the standard widgets. The medieval guide and how to make your own kingdom →","",11],[0,"the-same-screen-on-a-phone-and-on-a-desktop","The same screen on a phone and on a desktop",1,"The gallery shots are a phone in portrait. Below is the same code at 1280×800 — the content stops at a readable width (480dp) and centres, while the HUD stays in the corners. The form steps around floating HUD pieces, but does not move when it is already clear of them: pushing anyway would throw the content off-centre on a wide screen. default_dark · 1280×800 scifi_dark · 1280×800","",11],[0,"the-same-sheet-a-different-look","The same sheet, a different look",1,"Default — rounded card, sticky search row and footer Sci-fi — top accent edge, cut corners, glow Pick one from Project Settings → gohud → Theme → Preset, or set GoConfig.preset. Filling in theme, skin or icons explicitly wins over the preset, so you can take a preset and override just one of them. A new theme is one JSON file. new_theme.py kingdom --from medieval_dark writes a palette that spells out every inherited value; make_theme.py kingdom builds the theme, its control artwork and its skin dials, and the preset appears in the picker. Every setting and dial →","",11],[0,"skin","Skins — where a Theme cannot reach",0,"A skin owns the joystick, quick-slot faces, the coach-mark ring, chips, skeletons, alerts, dividers and section headings. Subclass it and override only what you want changed. Class Shapes it can make GoStyleBoxCut Diagonally cut corners (chamfer), one thickened accent edge, an outer glow GoStyleBoxBracket Corner marks only, without enclosing the content GoStyleBoxMedieval A forged frame with rivets, corner engraving, a bevel highlight and material grain All three serialise into a Theme resource — which is what lets a theme change shape, not just colour. GoSkinSciFi and GoSkinMedieval are the shipped subclasses. GoStyle.box() always returns a StyleBoxFlat, because calling code receives it and adjusts bg_color or corner_radius. Use GoStyle.surface() when a custom shape has to survive.","class_name MySkin extends GoSkin func slot_box(accent: Color, lit: bool) -> StyleBox: var box := GoStyleBoxCut.new() box.bg_color = accent box.cut = 6.0 box.edge_color = accent return box",-1],[0,"widgets","Widgets",0,"The pieces, in one table. Each one in detail → Class Base What it does GoSurface Control Floating card shell — centre, bottom or anchored placement, sticky header and footer, scrolling body, drag-to-resize GoSheet CanvasLayer Pages rising from the bottom, on their own layer so they sit above the HUD GoDialogs Node Confirmations and alerts you await; destructive for irreversible actions GoForm MarginContainer Width-capped forms that avoid the virtual keyboard and, with avoid_hud, the HUD GoScroll ScrollContainer Touch scrolling; the scrollbar tucks into the card padding GoNotice PanelContainer A snackbar that takes neither input nor focus GoPromptCard PanelContainer A question that does not block the game GoCoachMark Control A guided tour that points at real controls; pressing the target advances GoHudAnchor Control Pins HUD pieces to one of nine safe-area spots GoBar Control Health, mana and experience bars with eased changes GoSlot Button A quick slot — icon, quantity, cooldown and shortcut on one face GoJoystick Control Virtual stick in fixed, follow or relative mode GoIconButton Button Looks small, presses large GoStyle static Buttons, list rows, inputs, chips, tables, tabs and more, made one consistent way","",-1],[0,"examples","Examples you can run",0,"Three ways to see gohud before writing a line — none needs a server or an autoload. Example How to open What it shows Gallery examples/gallery/gallery.tscn · F6 Every widget, a picker for every installed preset, and the full icon set Medieval examples/medieval/medieval.tscn · F6 A character sheet, satchel and quest journal; buttons switch iron and parchment and open item details Demo app cd examples/demo && godot A home screen opening the gallery, the 15-chapter guided tour, the showcase screen and the medieval look, with live widgets and one card per class The demo app needs no setup: if the add-on link or the import cache is missing it says so, repairs the folder and reopens itself. Its home screen opens each example inside the same window — press 1–4 or click a row, and the bar across the top brings you back. In the tour, C toggles Cinema mode, F11 fullscreen, Space pauses (or plays the current widget while exploring), the arrows change chapters and Escape returns to Start. Using an AI coding agent? Open all three with one command →","cd examples/demo && godot # home screen; links and imports on first run bash examples/demo/run.sh # the same app, from the add-on root bash examples/demo/run.sh -- --open=gallery # skip the home screen bash examples/demo/run.sh -- --explore=surfaces # open straight onto one tour chapter bash examples/demo/run.sh --record /tmp/gohud-demo.avi # 1080p / 60 fps recording of the tour",-1],[0,"ai","Open it from your AI coding agent",0,"gohud ships a Claude Code plugin with slash commands. Type one and the gallery or the demo opens in its own Godot window — no editor to start, no scene to find.","",-1],[0,"install-the-commands-once","Install the commands once",1,"Other agents such as Codex can use the same skill: copy skills/gohud from this repository into the agent's skills folder.","claude plugin marketplace add thruthesky/gohud claude plugin install gohud@gohud # then restart Claude Code",18],[0,"gohud-preview-the-widget-gallery","/gohud:preview — the widget gallery",1,"Opens the gallery: every widget, a picker for all six presets and the full icon set. Type it inside your Godot project and it uses your addons/gohud; without one it uses the copy bundled with the plugin, or a fresh clone. It runs in a throwaway sandbox project, so your autoloads and import cache are left alone, and the window stays open until you close it.","",18],[0,"gohud-preview-demo-the-15-chapter-guided-tour","/gohud:preview demo — the 15-chapter guided tour",1,"Opens the demo on its start screen. Start demo plays the tour with real input; Explore widgets lets you try one widget by hand. The keys listed under Examples work here too.","",18],[0,"many-other-commands","Many other commands",1,"Command What it does /gohud:preview medieval The character sheet, satchel and quest journal in the medieval presets /gohud:preview demo --explore hud The demo opened straight onto one chapter — hud buttons inputs selection lists data states surfaces prompt touch coach scrolling forms anchors theming /gohud:preview gallery --preset scifi_dark The gallery in one preset; add --phone for a 390×844 phone window or --size 1920x1080 for any size /gohud:preview res://ui/main_menu.tscn A scene of your own project, run inside that project /gohud:preview list Every target, preset id and demo chapter key /gohud:preview gallery --check A headless smoke test with no window — fails on script errors /gohud:features Every feature, grouped, one line of code each /gohud:features hud One area in depth — surfaces hud style theming icons i18n accessibility config Or just ask. The skill teaches the agent gohud's whole API, so plain requests work too — \"Build a pause menu with gohud\". Installed as a plain skill in ~/.claude/skills/gohud, the commands take a space: /gohud preview demo, /gohud features. Previews need Godot 4.6 or newer on your PATH, or pass --godot /path/to/godot.","",18],[0,"readable","Readability is measured, not eyeballed",0,"\"Pretty\" is taste; \"legible\" can be measured. Faint grey text looks fine on a good monitor and disappears on a phone in daylight — picking by eye guarantees it eventually. Every colour pair and every button state's label over the panel it sits on is measured against WCAG, in all six themes. Translucent colours are composited over their real backdrop first — measuring them directly reports a better ratio than the screen shows. Text colours are therefore derived, not chosen: the builder takes the palette entry as a starting point and pushes its lightness until it clears the threshold on every surface it can land on. Change the palette and the contrast follows. The rules and the traps behind them →","python3 addons/gohud/tools/check_contrast.py",-1],[0,"develop","Checks, releases and this site",0,"One entry point runs every check; each part sees something the others cannot. Check What it catches run_tests.sh at four screen sizes Widget behaviour and layout, RTL positions, keyboard focus, runtime skin contrast, every preset new_project_check.sh Hidden dependencies on a host project; with --zip the release as shipped, with --export a Web build check_contrast.py WCAG contrast of every theme file, button states and translucent panels included check_generated.py · check_scaffold.sh Generated themes match their palettes; a throwaway theme builds and passes contrast check_package.py Version bumps, changelog moves and ZIP contents, in temporary copies check_site.py This site's links, anchors, page language, glossary and dial tables Latest recorded run — 2026-09-13, Godot 4.7.2, macOS, Compatibility renderer. 438 of 438 checks at 390×844, 844×390, 768×1024 and 1280×800 in an empty project; 438 of 438 again with the GoRuntime autoload; a Web export; zero contrast failures across the six themes. Godot 4.6 and physical Android or iOS devices were not part of this run.","bash addons/gohud/tools/check_all.sh",-1],[0,"release-versions","Release versions",1,"A successful run updates plugin.cfg, GoUi.VERSION and CHANGELOG.md together, moving the Unreleased notes into a dated entry. A failed run changes nothing, and an existing ZIP is never overwritten. The ZIP leaves out this website, the tests and the tools.","bash addons/gohud/tools/package.sh # 1.0.1 → 1.0.2 · builds/1.0.2/gohud-1.0.2.zip bash addons/gohud/tools/package.sh --increase-minor-version # 1.0.1 → 1.1.0",24],[0,"this-site","This site",1,"The pages are plain HTML in www/ — English at the root, Korean in ko/. A GitHub Actions workflow publishes that folder as the site root.","python3 addons/gohud/tools/make_site.py # glossary and skin-dial tables, generated from source python3 addons/gohud/tools/check_site.py # links, anchors, language, glossary, generated tables bash addons/gohud/tools/site_shots.sh /tmp/shots # desktop and phone screenshots of every page",24],[1,"layers","Three layers",0,"A preset bundles all three. Fields you leave empty fall back to the defaults. Layer Decides How you change it Theme Colours, sizes, text scale, and the shape of everything the engine draws — button panels, inputs, tabs, toggle artwork Author a .tres, or generate one with tools/make_theme.py GoSkin The shape of everything code draws — the joystick, quick-slot faces, the coach-mark ring, chips, skeletons, dividers, section headings Subclass it and override only the methods you care about GoIconSet Icons by name — 84 default drawings, or the medieval set's 16 engraved drawings over them. A texture pack or an icon font; the calling code is identical either way Supply a set, or override just a few names Why a Theme alone is not enough. Rounded is the only corner StyleBoxFlat has — set the radius to zero and you get a right angle; there is nothing in between. Diagonally cut corners, a single thickened edge, an outer glow: the engine draws none of them. And the joystick and coach mark are drawn in _draw(), where a theme cannot reach at all.","",-1],[1,"pick","Picking one",0,"That one line swaps theme, skin and icons. From the editor, use Project Settings → Gohud → Theme → Preset; from a settings resource, GoConfig.preset.","GoUi.use_preset(GoThemePresets.SCIFI_DARK)",-1],[1,"keeping-one-field-your-own","Keeping one field your own",1,"Setting theme, skin or icons explicitly wins over the preset.","GoUi.use_preset(GoThemePresets.SCIFI_DARK) GoUi.config.icons = my_icons # your icons, the preset's everything else",28],[1,"the-six-built-in-presets","The six built-in presets",1,"Preset Panels Skin · icons default_dark · default_light Rounded StyleBoxFlat panels on a graded shadow scale; primary buttons lift and sink when pressed GoSkin · 84 default icons scifi_dark · scifi_light Chamfered GoStyleBoxCut panels with a neon edge and glow; GoStyleBoxBracket focus marks GoSkinSciFi — hexagonal joystick · default icons medieval_dark · medieval_light Forged GoStyleBoxMedieval frames — iron and leather, or parchment — with rivets and corner ornaments on menus GoSkinMedieval · 16 engraved icons over the defaults","",28],[1,"medieval","Medieval: iron, leather and parchment",0,"medieval_dark pairs dark iron and leather with antique gold. medieval_light uses parchment and ink. Both use GoSkinMedieval, the existing widgets, and 16 original icons with engraving details; navigation icons fall back to the original set. Choose the preset before creating your UI. Explicit GoConfig.theme, skin or icons overrides still take precedence. Open res://addons/gohud/examples/medieval/medieval.tscn and press F6 for a working character sheet, satchel and quest journal. Its buttons switch variants and open item details. Iron and leather Parchment and ink Menu frames have a rivet and small corner ornament; the always-visible HUD keeps a quiet edge. HP, mana and stamina use red, blue and olive fills. Cinzel is scoped to titles and subtitles; body text keeps the host/default font. The bundled font includes its license at assets/fonts/cinzel/OFL.txt. The engraved set redraws bag, book, box, coin, crown, flag, heart, key, map, potion, shield, star, sword and user, and adds scroll and seal. Every other name falls back to the default set, so close and settings keep their default drawings in every preset. Cinzel covers Latin script; give localized headings a font of their own through shape.fonts. What the suite checks for both variants. The medieval skin is selected; menu and HUD panels are forged frames, with the ornament on menus only; titles use Cinzel while body text keeps the inherited font; engraved icons import as DPITexture; a real quick slot uses the skin; changing a copied skin's dials reaches the slot without touching the preset; and switching back restores the default and sci-fi geometry.","GoUi.use_preset(GoThemePresets.MEDIEVAL_DARK) # Or: GoUi.use_preset(GoThemePresets.MEDIEVAL_LIGHT)",-1],[1,"make-a-kingdom-of-your-own","Make a kingdom of your own",1,"JSON themes can inherit other JSON themes. Omitted values keep their parent's settings; circular inheritance is rejected. The generated preset keeps its parent's icon set. Setting Effect shape.material 0: iron · 1: leather · 2: parchment. Controls the surface marks. shape.grain_alpha Texture opacity. Use 0 for a clean surface. shape.ornament_scale · shape.bevel_strength Menu corner ornament size and bevel highlight intensity. shape.fonts Map title, subtitle, caption, body and button roles to addon-local res:// font files. Omitted roles keep their inherited font. skin.dials Quick-slot radius, rivet visibility, grain and highlight. The full table below lists each dial.","python3 addons/gohud/tools/new_theme.py kingdom --from medieval_dark # Edit themes/palettes/kingdom.json, then: python3 addons/gohud/tools/make_theme.py kingdom godot --headless --path . --import # In GDScript, before creating the widgets: GoUi.use_preset(&\"kingdom\")",31],[1,"tokens","Tokens",0,"Every colour and measurement lives under the GoHud type in the Theme, addressed by name. Not writing numbers is the rule: names follow along when the theme is swapped.","GoUi.color(GoTheme.ACCENT) # colour GoUi.metric(GoTheme.GAP) # measurement (dp) GoUi.font_size(GoTheme.ROLE_CAPTION) # size for a text role GoUi.box(GoTheme.BOX_CARD) # a copy of a StyleBox",-1],[1,"colours","Colours",1,"Name Used for background The screen floor surface · surface_soft · surface_high Three panel layers — panel, card, raised cell border Card and button borders text · secondary · muted Three text levels — body, supporting, dimmed accent · on_accent The accent and the text placed on it success · warning · danger · info Status text colours *_fill The same statuses as a fill — bars and other large areas scrim · shadow · track The veil behind popups, shadows, the bar's trough Why *_fill exists. The same meaning, opposite requirements. warning used as text must be dark enough to read on a light background; used as an experience bar's fill it must be bright enough to notice. One token cannot be both — the light theme's bar went brown. A theme without the _fill variant falls back to the base name automatically, so themes that predate these tokens keep working.","",33],[1,"measurements-dp","Measurements (dp)",1,"touch button_height gap_tiny gap_small gap gap_large padding padding_compact compact_padding_x compact_padding_y radius_small radius radius_large screen_margin scroll_deadzone scroll_edge scrollbar_width list_glyph icon_size notice_duration_ms","",33],[1,"text-roles","Text roles",1,"micro · compact · caption · body · button · subtitle · title — a role names a size, not a purpose. \"The largest text\", not \"the heading\". That way the scale holds even when a screen uses it for something else.","",33],[1,"type-variations","Type variations",1,"GoPanel GoCard GoButton GoPrimaryButton GoDangerButton GoDangerSolidButton GoBareButton GoCompactButton GoIconButton GoListButton GoTitleLabel GoSubtitleLabel GoCaptionLabel GoCompactLabel GoMicroLabel","",33],[1,"skin","Skins — where a Theme cannot reach",0,"Method Decides surface_box · floating_box · overlay_box Card and panel faces, floating surfaces, pills laid over the game or a map chip_box · chip_ink A chip's panel and the label colour that stays legible on it slot_box · slot_ink Quick-slot faces and their text disc_box The circle behind an avatar or badge alert_box · skeleton_box · segment_box Inline alerts, placeholders, segmented controls coach_ring_box The mark a coach tour puts around its target draw_joystick · draw_coach_pointer Things drawn directly — the joystick and its pointer divider_color · divider_thickness · section_box Dividers and section headings badge_box Quick-slot badges — quantity and time left progress_fill_box · notice_box · tint_notice Bar fills (outlined where a fill cannot reach 3:1 on its track) and snackbars","class_name MySkin extends GoSkin func draw_joystick(canvas: CanvasItem, center: Vector2, knob: Vector2, radius: float, knob_radius: float, ink: Color, base: Color, active: bool) -> void: # The widget has already worked out the positions — only drawing happens here canvas.draw_circle(center, radius, Color(base, 0.4)) canvas.draw_circle(knob, knob_radius, ink)",-1],[1,"shapes-styleboxflat-cannot-make","Shapes StyleBoxFlat cannot make",1,"Class What it draws GoStyleBoxCut Diagonally cut corners, one thickened accent edge, an outer glow GoStyleBoxBracket Corner marks only, leaving the content unenclosed GoStyleBoxMedieval A forged frame — rivets, corner engraving, a bevel highlight and iron, leather or parchment grain All three serialise into a Theme resource like any other StyleBox — which is what lets a theme change shape. Give padding through content_margin_*. Subclassing StyleBox in GDScript means _get_style_margin() is never called (measured on Godot 4.7). Fill the built-in fields or content will sit against the border.","",38],[1,"own","Your own preset — one file",0,"Adding a theme used to mean editing four places by hand — the builder's palette, the shape, a skin, a preset resource. One command now puts all of them in place. The first line writes themes/palettes/neon.json and themes/presets/neon.tres. The JSON carries every value of the theme it inherits from, fully spelled out, so it doubles as the list of what you can change: delete a key to keep the parent's value, write one to override it. Presets are found by scanning themes/presets/, so no registry code changes — the project setting's dropdown picks it up as well.","python3 addons/gohud/tools/new_theme.py neon --from scifi_dark --title \"Neon\" python3 addons/gohud/tools/make_theme.py neon # theme .tres + control artwork godot --headless --path . --import # import the new artwork once GoUi.use_preset(&\"neon\") # done — it shows up in the picker too",-1],[1,"what-you-can-change-on-one-page","What you can change — on one page",1,"Where What How from the parent theme dark · light · scifi_dark · medieval_dark · … or another JSON theme. Omitted keys inherit its values; an inheritance cycle fails palette background · three surfaces · border · three text tones · accent · four status colours · scrim · shadow · bar track \"#RRGGBB\", alpha as \"#RRGGBB@0.35\" fill-only vivid colours *_vivid keep bars saturated even when the text colour has to darken — leave one out and the builder says so readability not tuned by hand. Text, borders, the accent and the label on it are pushed by the builder until they clear the thresholds — change only the accent and the checks still pass shape kind flat (rounded) · cut (chamfered corners) · medieval (forged frames) sizes radius · radius_small · radius_large · gap · gap_small · gap_large · padding · button_height · button_padding[4] — the tokens follow, so panels and widget maths never disagree cut only cut_ratio · cut_max · corners (diagonal|all) · glow · edge medieval only material (0 iron · 1 leather · 2 parchment) · grain_alpha · ornament_scale · bevel_strength · fonts — title, subtitle, caption, body, button mapped to addon-local res:// font files skin base — the family for the places code draws itself (joystick, slots, badges, coach-mark ring, chips, dividers) default · scifi · medieval dials — the numbers of those places: slot border width and tint, badge padding, chip and alert opacity, joystick ring width and opacity, sci-fi chamfer sizes, glow and bracket Every parent value is spelled out in the JSON. The builder writes them into themes/skins/gohud_skin_<id>.tres — no skin code is touched. Only to change the drawings themselves do you run --new-skin, which scaffolds a GoSkin subclass icons the icon set a res:// path to a GoIconSet resource — kept from a JSON parent when omitted dark · title how the picker shows it boolean · string","",40],[1,"every-dial","Every dial",1,"The table below is filled by tools/make_site.py straight from the skin scripts' @export lines — add a dial or change a default and the table follows. Use these names under skin.dials in the JSON. A dial whose default reads 14 is an integer, one that reads 10.0 a float — write either form in the JSON and the builder casts to the script's type. Default skin GoSkin — 17 Dial Default What it sets chip_fill_alpha 0.16 Fill opacity of a chip's panel. chip_edge_alpha 0.45 Edge opacity of a chip's panel. alert_tint 0.1 How far an alert box's panel is tinted toward its status colour. slot_tint_lit 0.24 Accent tint of a quick-slot panel while its cooldown runs. slot_tint_idle 0.08 Accent tint of a quick-slot panel at rest. slot_border_lit 2 Quick-slot border width (dp) while its cooldown runs. slot_border_idle 1 Quick-slot border width (dp) at rest. badge_pad_x 5 Horizontal inner padding (dp) of a badge (quantity, time left). badge_pad_y 1 Vertical inner padding (dp) of a badge. badge_edge_alpha 0.6 Edge opacity of a badge's panel. float_shadow_alpha 0.45 Shadow opacity of floating cards (coach mark, prompt card). float_shadow_size 14 Shadow blur (dp) of floating cards. float_shadow_lift 4 Downward offset (dp) of a floating card's shadow. float_glow_size 10.0 Glow distance (dp) of a floating panel that glows instead of casting a shadow, like the sci-fi chamfered panel. joystick_base_alpha 0.42 Opacity of the joystick's base disc. joystick_ring_alpha 0.45 Opacity of the joystick's ring. joystick_ring_width 2.0 Width (dp) of the joystick's ring. Sci-fi skin GoSkinSciFi — 10 Dial Default What it sets cut_chip 7.0 Chamfer size (dp) of a chip's panel. cut_skeleton 5.0 Chamfer size (dp) of a skeleton panel. cut_alert 8.0 Chamfer size (dp) of an alert box. cut_segment 8.0 Chamfer size (dp) of a segmented control. cut_slot 6.0 Chamfer size (dp) of a quick-slot panel. cut_disc_ratio 0.24 Chamfer of avatars and discs = diameter × this ratio. slot_glow_alpha 0.45 Glow opacity of a slot whose cooldown is running. slot_glow_size 6.0 Glow distance (dp) of a slot whose cooldown is running. bracket_arm 12.0 Arm length (dp) of the coach mark's targeting bracket. bracket_thickness 2.0 Line thickness (dp) of the coach mark's targeting bracket. Medieval skin GoSkinMedieval — 5 Dial Default What it sets slot_radius 4.0 Corner radius (dp) of the medieval quick-slot frame. leather_grain_alpha 0.035 Opacity of the subtle leather marks inside quick slots. ornament_scale 1.0 Scale of the metal decorations at quick-slot corners. Menu frames use shape.ornament_scale. bevel_strength 0.18 Strength of the metal highlight along quick-slot edges. slot_rivets 1 Show small metal rivets on quick slots: 0 hides them, 1 shows them.","",40],[1,"subclass-the-skin-optional","Subclass the skin (optional)",1,"Only what theme and shape cannot reach — the joystick's hexagon, slot badges, the coach mark's targeting bracket — lives in a skin. Subclass GoSkin, override just those methods, and point the JSON's skin at that resource. A colour-only theme skips this step. A check keeps the promise. tools/check_scaffold.sh creates a throwaway theme every run — changes the accent, sets the radius to 4 — and verifies generation, token propagation and a clean contrast pass before removing it. The first run found a theme with only its accent changed failing 8 pairs: the builder pushed text colours but used the accent as written. It now pushes the accent and the label on it too.","",40],[1,"contrast","Readability is measured",0,"\"Pretty\" is taste; \"legible\" can be measured. Faint grey text looks fine on a good monitor and disappears on a phone in daylight — picking by eye guarantees that eventually. What Required Body text 4.5 : 1 Large text (24px and up) 3 : 1 Accent borders and icons 3 : 1 Decorative borders 2 : 1 — WCAG does not apply its 3:1 rule to decoration Adjacent surfaces 1.12 : 1 — enough that a card reads as separate from its background","python3 addons/gohud/tools/check_contrast.py",-1],[1,"same-hue-text-on-a-same-hue-tint","Same-hue text on a same-hue tint",1,"This trap appeared three times in this codebase — pressed buttons (accent panel, accent label), danger buttons, and chips. It looks good in a swatch and collapses the moment it is measured, because the two share a hue and so have no difference in lightness. Label colours are therefore not chosen by hand. The theme builder and GoSkin each push lightness — and only lightness — until the threshold is cleared on the panel the text actually sits on. Hue and saturation survive, so the palette keeps its character and changing it keeps its contrast.","",44],[1,"colour-read-as-text-and-colour-painted-as-area-w","Colour read as text and colour painted as area want opposite things",1,"On a light theme a warning colour has to darken to stay readable as text (#96500A). When the bar fill inherits that, the experience bar turns brown. Colour alone cannot fix it: yellow is intrinsically bright, so it clears 3:1 over no grey track at all, and darkening the track makes it worse (1.52 → 1.06). So the two are separated. Colour stays vivid — the palette carries fill-only colours such as warning_vivid. The edge carries the contrast — GoSkin draws a 1dp outline on any fill that cannot reach 3:1 against its track, so \"how full is it\" stays legible. Leaving it out is reported. Without a *_vivid, a fill that comes out dark makes the builder print 막대가 칙칙하다 — 팔레트에 warning_vivid 를 더한다. A place that used to degrade silently now says so.","MY_LIGHT = dict( warning=hexc(\"9A5407\"), # read as text warning_vivid=hexc(\"F2A007\"), # painted on the bar )",44],[1,"a-floating-panel-cannot-know-what-is-behind-it","A floating panel cannot know what is behind it",1,"A HUD is drawn over the game — a snowfield, or a cave. If its panel is translucent, whatever is behind it bleeds through and changes the panel's colour, taking the text contrast down with it. At 82% opacity the dark themes' muted text fell to 3.74:1 over a bright backdrop — below the minimum. So translucent panels are measured with pure white and pure black behind them; those are the worst a game scene can offer. The bundled themes' HUD panel is opaque enough to pass (92%) while keeping the glassy look. For the same reason icon colours are not fixed either. The quick slot's glyph was hard-coded white and measured 1.11:1 on a light theme — it now follows the panel it sits on, exactly as the labels do. Two checkers, two reaches. check_contrast.py reads the theme files; colours a skin mixes while running (chips, slots) are measured by the suite's skin contrast section inside Godot.","",44],[2,"surfaces","Floating windows",0,"","",-1],[2,"gosurface-the-shell","GoSurface — the shell",1,"Popups, sheets and dropdowns all use this one. It handles the safe area, the virtual keyboard, sticky header and footer, a scrolling body, which window is topmost, focus restoration, back-button ownership and drag-to-resize. Placement Looks like Used for CENTER A card in the middle Confirmations, settings BOTTOM A sheet rising from below Lists, management pages ANCHOR A card pinned beside a control Dropdowns, context menus It does not remove itself. Reacting to close_requested — hiding or freeing — is the owning screen's job, because the shell cannot know why it is closing (save and close, or discard).","",48],[2,"gosheet-a-page-from-the-bottom","GoSheet — a page from the bottom",1,"It owns a CanvasLayer, so it reliably sits above the HUD. open() clears the body and turns off the back button and sticky rows — a dead button on a screen with nowhere to go back to reads as broken.","var sheet := GoSheet.new() add_child(sheet) sheet.open(\"Inventory\") sheet.toolbar().add_child(GoStyle.line_edit(\"Search…\")) sheet.toolbar().visible = true sheet.footer().add_child(GoStyle.button(\"Close\", sheet.close, GoStyle.Tone.PRIMARY)) sheet.footer().visible = true",48],[2,"godialogs-confirmations-you-await","GoDialogs — confirmations you await",1,"confirm(title, body, ok_text, cancel_text, extra, args, destructive) — everything after the body is optional, and alert() works the same way with a single button. Set destructive for something that cannot be undone: the confirm button becomes the filled GoStyle.Tone.DANGER_SOLID button. On a light theme a tinted danger label has to darken until it reads as black text; the filled button keeps white text on red. The tone is re-applied on every call because one window is reused, and a second confirm() while one is open returns false at once. confirm_key() takes translation keys. Button layout. action_layout is VERTICAL by default (safe for long translations), HORIZONTAL for one row with half the width each, or AUTO, which uses one row only when both labels fit half the card on a single line and stacks them otherwise. action_gap is the space between the buttons and body_gap the space above them — make it wider than the button gap so the question and the choice read as two parts. For one dialog only, call set_next_action_layout() before opening it. The row is built when a dialog first opens, so an autoloaded GoDialogs adds nothing at startup.","if await dialogs.confirm(\"Delete save\", \"This cannot be undone.\"): delete_save()",48],[2,"goform-a-width-capped-form","GoForm — a width-capped form",1,"It guarantees its descendant labels wrap. Without that, one long sentence stretches into a single line whose minimum width exceeds the screen, and the sides are clipped — you cannot even tell what screen you are on. Turning wrapping on by hand per scene is forgotten every time.","",48],[2,"goscroll-touch-scrolling","GoScroll — touch scrolling",1,"Put one thing inside it. Headers and button rows belong outside — a long list must not push them off screen. use_panel_edge() moves the scrollbar into the card's existing padding while the content keeps its indent.","",48],[2,"messages","Telling the player something",0,"","",-1],[2,"gonotice-snackbar","GoNotice — snackbar",1,"It never intercepts input. A notice is read, not pressed. It takes no focus, does not block game input, and the button beneath it still presses. When confirmation is needed use GoDialogs; when a choice is needed, GoPromptCard.","",54],[2,"gopromptcard-a-question-that-does-not-block","GoPromptCard — a question that does not block",1,"Floats at one side with no scrim. The game keeps running.","",54],[2,"gocoachmark-guided-tour","GoCoachMark — guided tour",1,"Points at real controls one at a time. Only the card takes input, so pressing the highlighted control itself advances the tour — \"press here\" becomes an action rather than a sentence. title and body may be translation keys or plain text. A step whose target has disappeared is skipped.","tour.start([ {\"target\": bag_button, \"title\": \"Bag\", \"body\": \"Everything you pick up lands here.\"}, {\"target\": map_button, \"title\": \"Map\", \"body\": \"Press to open the full map.\"}, ])",54],[2,"hud","HUD",0,"","",-1],[2,"the-small-things-decide-whether-it-looks-good","The small things decide whether it looks good",1,"Polish the big panels all you like — what catches the eye is always something small: a word split in two, a glow chopped off, a cramped slot. Four such things were pointed out on 2026-09-13 and fixed in the widgets and themes, not in the demo screen, so every screen gets the same result. What showed Cause Fix The coach mark's Done became Don/e A button with wrapping on subtracts its text width from its minimum — squeeze it and it breaks inside a word GoStyle.fit_words(): one word never wraps; several words wrap, but the longest word always fits — judged on the displayed text, not the translation key, and re-applied by the form when the language changes A full-width button's glow cut off vertically on the left A scroll container clips at its edge, no exceptions. Only the right survived, thanks to the rail gutter GoScroll borrows the parent's padding to push its edge 12dp out and gives it back inside — content stays put Time, icon and count stacked vertically in a slot Three rows inside 48dp Icon large in the centre, shortcut top-left, count as a bottom-right badge, remaining time as a badge over the icon — badge panels come from the skin's badge_box() A flat dropdown with no visible selection Item hover used the button panel; radio marks were the engine's faint default A dedicated hover panel (soft accent tint), the same radio artwork as checkboxes, item height sized for touch Dropdown — hover panel and radio mark Slots — corner badges and remaining time (sci-fi) Give icon-only buttons a description with GoStyle.icon_button(icon, action, -1, tooltip_key) — the tooltip for mouse users and the accessibility name for screen readers both come from that one value. gohud draws the tooltip itself: the engine's default splits the text one character per line when its width calculation goes wrong (measured at 1dp wide). The coach-mark card keeps off fixtures In landscape the card used to be sent to the very top or bottom edge — exactly where headers and HUD pieces live; in the demo it sat on the header's → ×. It now stays level with its target, steps around any GoHudAnchor that reserves space, and takes other things to avoid via keep_clear. It moves the shortest distance that clears the obstacle without covering the target, and stays put when the screen is too small to clear everything — overlapping beats vanishing. Its depth — so it reads as above the page — comes from skin dials (float_shadow_size, float_shadow_lift, float_shadow_alpha; chamfered skins use float_glow_size).","tour.keep_clear = [header_bar, toolbar] # fixtures that are not anchors",58],[2,"screens-with-a-mouse-and-a-keyboard","Screens with a mouse and a keyboard",1,"A finger has no hover state. Build against mobile only and two desktop-only states go unchecked — and one of them was broken: gohud's focus ring was invisible on a primary button. The panel is the accent colour and so was the ring: 1.00:1, the same colour drawn over itself. Moving by keyboard, there was no way to tell where you were. Top to bottom: normal · hover · focus. Filled buttons get a ring that contrasts with their panel. Filled buttons (GoPrimaryButton, GoDangerSolidButton) use a ring that contrasts with their own panel — dark on a dark theme, white on a light one, a dark targeting bracket in sci-fi. The contrast checker measures the focus ring against the panel it sits on, so the rule survives a palette change (WCAG 2.4.11 asks for 3:1 on focus indicators).","",58],[2,"gohudanchor-nine-spots","GoHudAnchor — nine spots",1,"Respects the safe area and edge margins. Give it a landscape_spot and it moves in landscape — controls that sit bottom-centre in portrait can go bottom-right instead. Why it exists. Written by hand per HUD piece, anchors and offsets drift: a health bar ends up under the camera cutout on a notched device, and a control button lands on the gesture bar in landscape. That arithmetic lives in one place. Keeping content out from under the HUD When a scrolling screen shares the display with a HUD, the content slides underneath it and the two sets of text overlap until neither can be read. It happened twice in this add-on: an input's placeholder squeezed out between the quick slots, and a settings toggle's knob landed on the health bar. Each rectangle is avoided in whichever direction costs the least area. A bar in the top-right is stepped around downwards on a portrait phone (13% of the height) and sideways in landscape (21% of the width) — avoiding it vertically in landscape would cost 27% of the screen. Ground already given up is subtracted before the next rectangle is measured. HUD pieces collide with each other too The nine spots divide the screen; they do not guarantee the pieces miss each other. A wide snackbar at top-centre landed squarely on the health bar at top-right, hiding the value — you could not read how much health was left. It steps aside vertically only — moving sideways as well would make a centred toast appear in a different place each time it shows. And it only avoids pieces that are not stepping aside themselves: if both yield, the two swap places forever.","form.avoid_hud = true # keep clear of every visible GoHudAnchor joystick_anchor.reserve_space = false # …except ones that only appear under a finger notice_anchor.avoid_peers = true # settles below the fixed HUD notice_anchor.reserve_space = false # and does not push the page while it is up",58],[2,"gobar-health-mana-experience","GoBar — health, mana, experience",1,"Changes ease over 0.18s — a health bar that jumps does not tell you how hard you were hit. reduce_motion makes it instant. Large numbers abbreviate to 12.3k so the bar's width does not jitter as digits come and go.","var hp := GoBar.new() hp.label_text = \"HP\" hp.ink = GoUi.color(GoTheme.DANGER_FILL) # the fill-only token hp.set_values(320, 500) # \"320 / 500\"",58],[2,"goslot-one-quick-slot","GoSlot — one quick slot",1,"Icon, quantity, cooldown and shortcut on a single face. Hanging badges off a small circle makes each slot a different width and the row wobbles. Visible size and touch size differ. Packed slots have overlapping 48dp touch boxes; the overlap goes to whichever slot's centre is nearer — tell them about each other through touch_peers. keyboard_focus is off by default, so a row of eight slots does not sit between a keyboard user and every settings control in the Tab order. Turn it on when a keyboard or gamepad is the only input.","",58],[2,"gojoystick-virtual-stick","GoJoystick — virtual stick",1,"Mode Behaviour FIXED Always the same place — easy to find without looking FOLLOW Appears where the thumb lands RELATIVE Keeps following the thumb after appearing — comfortable for long movement The Vector2 from moved has length 0–1, so you can multiply it straight into a speed; it does not vary with screen size or DPI.","",58],[2,"goiconbutton-looks-small-presses-large","GoIconButton — looks small, presses large",1,"Only safe while its siblings ignore input. If the widened area covers the next button, that button stops responding. Side-by-side icon buttons need touch_peers.","",58],[2,"factory","GoStyle — one place things are made",0,"Call Button.new() directly and padding, height and wrapping drift slightly from screen to screen. That drift is invisible in code review and obvious in a screenshot. Group Functions Structure column row wrap_row padding spacer divider responsive_grid aspect foldable Text label label_key line section typography Buttons button button_key icon_button list_button list_row apply_icon style_choice_card style_chip_button style_chip_label Input line_edit textarea toggle checkbox slider picker select dropdown radio_group segmented choice_grid Display card chip hud_panel chip_panel avatar skeleton alert table tabs breadcrumb progress empty_state Surface pieces surface box floating disc face_padding Helpers form fit_words natural_width tooltip_node card_body let_input_through box() always returns a StyleBoxFlat, because callers receive it and adjust bg_color or corner_radius. Use surface() when a custom shape has to survive.","",-1],[2,"list-rows-with-a-description","List rows with a description",1,"A row with a second line gets equal top and bottom padding, roomier than a single-line row, and the title/description pair stays vertically centred beside the icon — also when a container stretches the row taller, and after the description wraps onto several lines. The whole row remains one tap target. Two-line rows, slots and bars in the medieval example — the same factory calls under medieval_light.","GoStyle.list_button(GoIconSet.SWORD, \"Oathkeeper\", _inspect, Color.TRANSPARENT, \"Longsword / Attack 42\", false)",66],[2,"flowing-rows","Flowing rows",1,"Put controls into wrap_row at their natural width. With wrapping left on, a button's minimum width drops to almost nothing and the row sizes its cell by it — the label collapses to one character per line. The factory prevents this for you.","",66],[2,"never-leave-an-empty-list-empty","Never leave an empty list empty",1,"People read a blank screen as broken.","page.add_child(GoStyle.empty_state(GoIconSet.BOX, \"Nothing here yet\"))",66],[2,"hooks","Subclass hooks",0,"Every place a child widget is constructed goes through an overridable method, so a project that subclasses gohud types can get its own subclasses inside the widgets without copying code. Hook Widget Default _make_scroll() GoCoachMark, GoSurface GoScroll.new() _make_close_button() GoPromptCard, GoSurface GoIconButton.new() _make_surface() GoSheet, GoDialogs GoSurface.new() _should_pause() GoCoachMark Hides the card while a modal is open","",-1]]};
